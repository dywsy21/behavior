{
  "experiment": "H09-S1-semantic-action-lora",
  "owner": "Astra-max-vlm-sft-showharness",
  "hypothesis": "A small VLM can learn a reusable discrete motion interface from audited, same-state expert motion segments, improving held-out semantic motion decisions and bounded closed-loop behavior over the identical frozen base model.",
  "tasks": [0, 1, 3],
  "seed": 41,
  "train_episodes_per_task": 12,
  "validation_episodes_per_task": 2,
  "test_episodes_per_task": 2,
  "max_samples_per_episode": 96,
  "max_training_samples": 3456,
  "horizon_frames": 16,
  "minimum_sample_spacing_frames": 16,
  "original_eval_tail_per_task": 10,
  "exclude_development_instances": [[0, 138], [3, 242]],
  "base_model": "/mnt/sdc1/robodojo/behavior_dev/semantic_agent_20260917/models/Qwen3.5-2B",
  "gpu": 1,
  "lora_rank": 64,
  "lora_alpha": 128,
  "lora_dropout": 0.05,
  "freeze_vision_encoder": true,
  "freeze_multimodal_projector": true,
  "image_max_pixels_per_view": 65536,
  "max_optimizer_updates": 600,
  "max_training_wall_seconds": 10800,
  "effective_batch_size": 16,
  "learning_rate": 0.0001,
  "warmup_ratio": 0.1,
  "checkpoints_at_updates": [200, 400, 600],
  "max_new_disk_gib": 8,
  "minimum_filesystem_free_gib": 80,
  "stop_conditions": ["nonfinite loss or gradients", "incorrect supervision mask", "unexpected trainable parameters", "split or image alignment failure", "CUDA OOM", "disk reserve below 80 GiB", "time or update budget exhausted"],
  "closed_loop_initial_budget": {"paired_starts": 2, "arms": ["base", "finetuned"], "max_decisions_per_run": 40, "max_controls_per_run": 1280, "max_seconds_per_run": 1200, "environment_seed": 0, "policy_seed": 41},
  "notes": ["No full-task success claim from local decisions or loss.", "No public_test or original per-task 5-percent holdout in training.", "Training is conditional on image/label review and gradient/template gates.", "All output modules remain in scripts/vlm_sft; no active harness edits."]
}
