# A2-5000 收音机低层隔离诊断：本人复核

2026-09-11；全部由当前主代理本人执行和查看，无子代理。此项不是完整任务成功率评测。

## 已确认的结果

使用原 train episode121 / instance138 的 448 步原始动作前缀，随后只给 A2-5000 正确的原始 GRASP 条件，最多 80×16 个模型动作。高层调用为 0；保留真实六帧、stride16、三相机和全部本体通道。权重 SHA：`b57eed704179a1517f6582b7572c975f732d3bc3c49d80ce119eaa2f643ae640`。

实际执行 1280 个 A2 动作、80 次模型调用。全部 80 个动作段末物理记录仍为 IN_PROGRESS，没有稳定抓取成功；official terminal=false。1729 次真实物理观察均被捕获，全部 80 次模型输入的历史图像/本体字节与独立记录逐一匹配，没有填充假历史、未来帧或高层提示。不能从段末记录排除两个记录点之间的短暂接触或持有。

诊断目录（robo）：`/mnt/sdc1/robodojo/behavior_dev/direct_execution_L6rqZ6_20260910/a2_prefix_radio_e121_l1_v1`。

## 我实际看了什么

- 全视频 28 个准确抽帧面板，覆盖帧 0 到 864；视频 865 帧、15 fps、57.6667 秒，前 14.9333 秒属于原始动作前缀，不计入模型贡献。
- 事件 1/21/41/61/81 的六帧×三相机历史：90 个图像面板。
- 原始演示与独立原始动作回放在源帧 448 和 970 的三相机对照：12 个面板。肉眼几何/操作阶段接近，不宣称仿真状态逐字节相同。

本地视频：`/home/wsy/behavior/memlite-results-20260911/A2-5000-oracle-radio-e121/rollout.mp4`。完整视频 SHA：`8203241e17bc45fd7e390723441f7d7e61916ac4380e8288ce577888b490f7e9`。

实际画面显示手部逐渐接近收音机附近，但最后的相对位置和夹取没有形成稳定抓持。这个局部窗口不是一直绕房间。右腕在较早阶段已经靠近对象，不能因头部视频后段才看到手臂，就说手臂直到那时才移动。也不能仅凭物体出现在图像中断言模型内部正确识别了目标。

## 将预测问题和执行问题分开

已核对本轮使用的真实机器人配置：双臂是未缩放、非 delta 的绝对关节位置控制。因此可以比较每段最后一条关节指令和执行后的实际关节位置，而不能用“指令变化 RMS / 实际变化 RMS”当控制器增益证据。

| 指标 | A2 的实际结果 |
|---|---:|
| 右臂段末关节跟踪 RMS 误差中位数 | 0.0002351 rad |
| 右臂该误差第 95 百分位 | 0.0005329 rad |
| 右臂该误差最大值 | 0.0008756 rad |
| 左臂段末误差中位数 | 0.0001421 rad |
| 首次闭爪指令（动作源帧，从 0 开始） | 1518 |
| 原始演示对应首次闭爪指令 | 952 |

A2 的闭爪指令连续维持至该诊断结束，共 210 动作。右夹爪的实际两个手指位置从约 0.05，在动作计数 1520 时降至 0.0314/0.0325，在 1536 时已接近 0；所以不是没有收到或没有执行闭爪指令。夹爪闭合依然不等于抓住目标。

同一 448–1120 动作区间，原始动作回放的右臂段末跟踪误差中位数为 0.0000286 rad，第 95 百分位 0.0015544 rad；原始动作确实能在该仿真中稳定抓取并最终开机。但模型和演示在接管后走过不同的接触状态，不能把这项比较宣称为严格同状态控制器消融。

**目前证据更支持：模型预测的接近轨迹与闭爪时机不够好，执行器却基本忠实跟随了这些指令。** 这缩小了原因范围，不代表已经找到全部训练根因，也不代表调整闭爪时间常数就能修复。下一项应在原始成功回放的真实观测上测 A2 的动作预测，区分成功状态附近的预测偏差和闭环累计偏离，随后才决定动作训练的修正。

## 边界和凭据

- `actual_analysis_v1.json`：真实历史、权重、控制与物理记录的关联核验。
- `execution_tracking_v1.json` SHA：`90d438956d86351cc4d14e476f1cacff6aeb012aed44afd6a0163b5148e67f4a`。
- `collection_result.json` SHA：`01a52a70d5fb7b5b7284a29cc73b0f3bf7401cdca6987f700ac22abe9dee360d`。
- 此诊断不生成纠正教师，不作训练准入，不改变旧失败结果，不证明完整任务 SR 改善。

## 2026-09-12：原成功状态上的实际预测已完成

在原始成功动作回放的 42 个真实状态（448、464、…、1104）上，各用 seed17/29 预测下一段动作，另重复首状态的 seed17，共 85 次模型调用。所有输入是过去六帧，原未来动作只参与输出后的误差比较；这些预测未在物理中执行。同状态同 seed 的首状态重复逐字节一致。

两个 seed 首次输出强闭合的源帧分别为 960 和 944，原动作是 952。这说明模型在正确接近状态附近能表达闭爪，不能把闭环1518才闭合解释成完全没学到闭爪。

但也不能反过来说成功状态上的预测完全正确。只看原下一段确实需要右臂运动的13个状态（未来16动作相对当前关节位置RMS≥0.005rad），预测沿原动作进展方向的投影增益中位数分别仅0.01838和0.01778；预测误差优于“保持当前关节位置”的只有6/13和4/13。换句话说，除闭环偏离以外，专家状态上的有效动作进展也不足。原轨迹不是唯一正确续行，投影增益更不是应直接施加的控制放大倍数。

新增证据：robo 的 `a2_original_radio_replay_state_predictions_v1/result.json` 和 `prediction_analysis_v1.json`；85次真实调用完成，0新physics/optimizer/数据准入。下一步比较A3相同观测上的动作进展与实际闭环，不能只看固定FM下降。
