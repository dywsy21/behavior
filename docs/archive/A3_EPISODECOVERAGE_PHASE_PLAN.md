# A3：修正实际采样覆盖的新低层训练阶段

2026-09-11。目标是纠正已证明的训练暴露缺口，继续提高完整任务成功率；不是承诺只改采样就能解决收音机和捡垃圾的抓取问题。

父权重为A2最终5000，SHA `b57eed704179a1517f6582b7572c975f732d3bc3c49d80ce119eaa2f643ae640`。完整保留动作专家、基座及192个LoRA条目。新阶段必须使用`pretrained_ckpt`，`resume_ckpt=null`，重新开始optimizer/scheduler/RNG/sampler计数。它不是等价续训；训练量与学习率也改变，不能把任何后续提升单独归因于采样。

拟定训练为四张卡、每卡microbatch 2、累积2次，global batch仍16；5000次优化器更新，学习率1e-5。保持三相机×六帧、FM预测32/执行16、完整底盘/躯干/双臂/夹爪、原950/50实例切分和原归一化统计。只打开显式episode轮转采样，不加入任何未准入C1/C2或public_test记录。较小microbatch用于给正在运行的独立A2评测留显存；训练前按实测峰值另留10GiB逐卡检查。

每100步继续原fixed-80诊断，每1000步保存完整checkpoint；预计五份约83GB，启动前检查可用空间，不删除历史证据。梯度累计后的训练日志目前记录最后一个microbatch的loss，不冒充两个microbatch的平均值；固定eval诊断口径保持一致。

启动门依次是：真实新采样/processor检查 → 真实父权重逐字节回载及两次累积更新、冻结/显存审计 → 原launcher解析两套准确配置 → 四卡5次更新和完整checkpoint回读 → 才启动独立5000步run。小跑权重全部丢弃，正式run重新从原A2父权重开始，不偷偷叠加小跑更新。

第一版GPU预检在读取训练模型上并不存在的serving专用诊断属性时退出，发生在创建优化器及任何更新之前。第二版直接核对全部1138个实际参数/缓冲区条目，含192个LoRA条目，全部与父权重逐字节一致；真实四个microbatch、两次优化器更新通过，冻结部分不变，Adam计数均为2；未保存这些诊断权重。峰值分配32,459,350,016字节，峰值保留32,631,685,120字节。完整结果SHA `cd0acb097feacf7477c7a4e85f0d36436a07c5a4dc4a5de2074e6670f428fa13`。

真实processor检查分别完成20条batch4及10条batch2训练样本，均覆盖五任务和三类此前漏训的任务轨迹，无CUDA/模型/优化器调用。正确的推理oracle源环境下，参数字节/观察复制等17项fixture通过；第一次把训练源用到simulator parser测试的环境错误保留，不改生产源来迁就测试。

smoke/formal两套真实配置已由原launcher解析通过。候选source SHA `356c717fe4f44d40517a1879e6742f12c9300daa5de13e3a89c71d1fa2fc9281`。四卡5步小跑已正常完成，11:39:52 UTC保存step5，checkpoint SHA `dfae4b5c4fe593449f6b9b7c0552df7014e72d68e42251a68fb7a91761736335`。本人真实CPU回读1138条状态，624条冻结状态与父权重逐字节相同、504条实际训练条目变化；504份Adam计数全5、scheduler5、下一microbatch10，四rank RNG完整。80条实际样本覆盖五任务，全部10个microbatch/rank及累积更新计数正确；原归一化统计不变。结果为 `a3_actual_saved_smoke5_inspection_v1.json`，不是成功率证据。

上述门通过后，已通过原launcher提交新独立run `formal_a3_episodecoverage_5000_v1`，启动PID3589152。仍须检查实际训练进程、早期更新和资源情况；提交启动不等于完成训练。现有A2五任务完整评测不热改、不因A3准备而提前终止。之后仍需真正的普通技能服从、反馈头训练、物理纠正和独立完整SR验证，不能以本轮训练步数作为goal完成依据。
