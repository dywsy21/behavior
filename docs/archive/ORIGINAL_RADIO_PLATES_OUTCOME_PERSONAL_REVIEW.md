# 原始回放物理反馈候选：125 条本人抽样复核

2026-09-11；当前主代理亲自查看，不由脚本或子代理代签。本报告不是模型可部署许可，也不是纠正动作数据准入。

## 数据和抽样范围

来自两个已实际执行、原始动作逐条核验过的 train 回放：收音机 episode121 / instance138，盘子 episode762 / instance242。候选共 715 条：482 UNKNOWN、174 IN_PROGRESS、59 SUCCEEDED；只有后两类的 233 条候选带监督 mask。UNKNOWN 不被伪造为真实失败，未构造 FAILED。

新候选格式为 `original_replay_past_physical_outcome_candidate_v1`，明确不是 C1 模型轨迹，也不是在模型失败状态上的纠正教师。候选 SHA：`70df77a3b075969edc59a97e174feabe86352cf8fceef017bdb947c6c2322448`。

我实际查看了：

- 125 条不同候选的最新三相机：375 面板，`current_00.jpg` 至 `current_24.jpg` 全部。
- 18 条完成前后关键候选的六帧三相机：324 面板。索引为 54、55、67、204、248、249、360、361、435、436、555、556、630、631、688、689、704、705。
- 合计 699 个展示面板，其中历史中的最新帧会与当前帧复核重复；不是 699 个独立样本。

抽样包含全部 59 条 SUCCEEDED 候选，其他每个“来源轨迹—原技能段—结果类”分组的第一、中间、最后候选。125 个精确索引及图像 SHA 见展示清单，SHA：`faded30424e351570593c1369bdb38fed8514c8fcd92df3b6b1ae8ce8bf032c8`。

本地面板和清单：`/home/wsy/behavior/memlite-resume.L6rqZ6/original_radio_plates_outcome_personal_panels_v1`。

## 逐类、逐边界观察

1. 收音机：从导航靠近桌面、右手伸到收音机周围，到闭合手指、抬起及左手接近按钮，三相机时序连贯。候选 54（obs961 / evidence960）仍 IP，55（977/976）才为稳定抓取成功。PRESS 的三个末端候选仍 IP，不因为画面接近按钮或后续 official success 就提前改成功。
2. 第一只盘子 plate93：候选 204（2305/2304）仍未确认抓稳，205（2337/2336）已转 NAVIGATE、画面开始持盘。这与原始 GRASP 标注在 2317 结束、实际回放在 2321 首次确认抓住的边界差异一致；没有把最后一个 GRASP 样本强行补成功。候选 248（3041/3040）为放置进行中，249（3057/3056）为实际稳定放入后的成功。
3. 第二只盘子 plate94：360（4865/4864）仍 IP，361（4881/4880）SUC；可见夹取及抬盘。搬运中盘上的食物发生变化，并不自动否定“持有目标盘子”的谓词，也不能拿局部持盘成功代替完整食物整理成功。435（6097/6096）仍放置中，436（6113/6112）起成功，之后手移开而盘留在冰箱。
4. bowl91：555（8081/8080）尚在接近/闭爪，556（8097/8096）达到稳定左手抓取；之后导航去另一个碗时仍可见左手持碗。
5. bowl92：630 的 obs9313 已接近稳定边界，但标签描述的是 evidence9312——当时仅连续确认 5 帧，仍 IP；631（9329/9328）才 SUC。三相机显示右手抬起第二只碗，不能将一帧后的画面倒灌给前一帧的监督标签。
6. 放碗：688（10273/10272）刚释放、尚未达到稳定时长，689（10289/10288）为稳定放入水槽；左手之后才放 bowl91，704（10545/10544）IP，705（10561/10560）SUC。主动松爪不是掉落失败，左右碗和顺序没有在所选样本中混置。
7. 导航、冰箱开门和关门候选目前均保留 UNKNOWN 且 mask=false；可见门打开/关闭不等于已经有精确到该右门关节的物理标签。不可将“不具备判定信息”作为一个已知物理失败类别来训练。

## 时间和模型输入检查

每个候选 observation anchor 为 `16k+1`，六个真实观察相隔 16 步，物理监督止于 `anchor−1`；最近 16 个已经执行的原始控制和最后观察属于同一已安装技能。跨技能控制段不混入。图像历史可包含此前技能的真实过去，这不是标签穿越。

模型只得到既定七个可观察字段。物理谓词、候选类别、成功时刻、来源审计和未来动作留在监督/审计侧；存储器先从完整原始控制文件选择过去 16 步，未来切片不进模型。

已实际用最终 B 的配置、normalizer 和原输入适配器，在 CPU 上处理全部 125 条抽样：每条都形成三路 `[1,6,3,256,256]` 图像，observable-only 前缀校验通过。结果为 `original_outcome_actual_B_processor_v1.json`。这是实际预处理，不是 VLM 预测、反馈头训练或校准。

## 本人结论和保留限制

所查看的 125 条候选中，未发现尚未处理的关键相机混置、目标串位、未来结果泄漏或把动作段结束伪装成物理成功的问题。对临界成功时长、主动释放和原始技能边界差异已逐项复核。这是有范围的本人抽样结论，不是对所有可能样本的保证。

仍不能直接宣布反馈头可训练/可部署：正例只来自两个 train 实例，存在大量同一成功事件的近邻重复；还缺独立的跨技能校准数据、明确真实失败及恢复覆盖。后续需做独立的数据准入、按实例隔离的校准、按事件而非按重复帧加权，并保持未知范围拒答。原候选及展示清单的 pending/`training_admissible=false` 保留，本报告不自动改写这些状态。
