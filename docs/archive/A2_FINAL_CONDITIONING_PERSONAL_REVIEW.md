# A2 最终5000步：意图—动作响应复核

2026-09-11；本人执行和复核，无子代理。这是实际模型输出的数值诊断，不是物理服从率、任务成功率或训练数据准入。

## 结论

**最终5000步仍没有显示明显增强的条件控制。** 同一批实际观察、相同随机种子下，更换技能/目标的动作影响仍很小。FM loss降低、模型正确载入、LoRA确实训练，都不能替代“低层会按意图做出不同且正确的动作”的证据。

## 实际验证

- 最终A2权重SHA：`b57eed704179a1517f6582b7572c975f732d3bc3c49d80ce119eaa2f643ae640`；1138个state条目、含192个adapter，全部真实读回逐位相等。
- 使用旧A与A2-2500已经测过的15个真实保存状态，同样的6帧×3相机、语义条件、seed17/29，共69次实际FM推理。15/15同条件同seed重复逐位相同。
- 汇总不是直接抄旧统计：重新校验三组结果的每个动作文件SHA，读入原始16×23动作，再计算每个控制组的差异；重算值与原报告一致。
- 对已知演示回放初态不一致的W12，保留原始诊断但从主要结论排除。主表为剩余12状态；并不是12个独立实例。

## 更换真实同任务技能条件

指标：每个状态上“只换技能条件的动作RMS差÷只换seed的动作RMS差”，然后取状态中位数。百分数不是错误率、服从率或成功率。

|控制组|旧A5000（单帧）|A2-2500（六帧）|A2-5000（六帧）|
|---|---:|---:|---:|
|底盘速度|1.165%|2.220%|1.296%|
|躯干位置|1.258%|1.251%|1.931%|
|左臂位置|2.916%|2.651%|2.618%|
|左夹爪|0.366%|0.457%|0.544%|
|右臂位置|3.354%|2.298%|2.267%|
|右夹爪|0.563%|0.835%|0.950%|

仅替换目标的主要6状态，A2-5000各组为底盘0.691%、躯干0.462%、左臂1.128%、左夹爪0.385%、右臂0.975%、右夹爪0.628%。同样没有出现明显的大幅目标响应。

这些是响应大小，不评价换条件后的动作是否物理正确。少量、特定失败附近状态也不能证明模型在所有状态都忽略文本。旧A到A2同时改变历史和训练参数，不是单独LoRA消融；即使不同组有小幅升降，也不能据此单独归因。

## 对下一步的影响

1. 继续已冻结的五任务完整仿真，独立统计实际子目标和官方任务成功，不能由上述数字代替。
2. 若闭环同样不能完成抓取，优先检查学习数据中技能切换/对象选择是否真正提供不同控制的监督，以及接触状态下的可验证纠正；不能仅加训练步数或将更多进行中帧填成失败类。
3. 当前反事实条件只供诊断，没有证实在接收状态可行。禁止把它们与原动作配对做“反事实BC”，也不能先假定更换文字就应立即大幅运动。
4. 两个训练收音机实例的独立G0.5接管都没确认抓取成功，因此不能依赖一个尚不可靠的“教师”盲目扩充低层纠正监督。接管的视觉、实际持有和源状态都要继续分别核对。

原始结果：`/mnt/sdc1/robodojo/behavior_dev/direct_execution_L6rqZ6_20260910/a2_step5000_actual_action_interventions_v1/result.json`，SHA `49605fc8e5e03a549eb145aa2d330f84b6f41bc39ab81c730bcb93acf8cac752`。

三组重算：[本地完整统计](/home/wsy/behavior/memlite-resume.L6rqZ6/a2_final_condition_comparison_v1.json)。本检查无optimizer更新、无simulator step、无训练数据准入，不接入正在评测的A2/B服务会话。
