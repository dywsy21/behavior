# 同状态接管首例：本人视觉与物理证据复核

2026-09-11；本人直接检查，无子代理。此为训练实例上的局部纠正尝试，不是完整任务成功率。

## 结论

**不准入成功纠正训练数据。** 旧低层 A 未完成抓取；独立原始 G0.5 从同一实际状态接管，也没有取得可验证抓取成功。保留为未成功接管的诊断证据，不能伪标 FAILED 物理类别，也不能把 G0.5 自动认证为教师。

## 证据和时间段

- 远程原始目录：`/mnt/sdc1/robodojo/behavior_dev/direct_execution_L6rqZ6_20260910/c2_same_live_state_pilot_v1/c1v2-matched-t0-train-e121-f448-grasp`。
- 本地视频：[完整录像](/home/wsy/behavior/memlite-results-20260911/C2-radio-train-e121/rollout.mp4)，100.33 秒、1505 帧、15 fps。
- task 0，demo episode 121，官方 train instance 138；原逻辑 split=train，无 eval 回灌。
- 0–约14.93秒：448 个真实演示前缀动作，仅用于抵达预定训练窗口。
- 约14.93–57.60秒：旧 A 的1280个实际控制，给定局部意图为 GRASP radio_89。
- 约57.60–100.27秒：独立 G0.5 执行1280个真实控制；它只接完整任务与实际RGB/本体状态，不接特权意图或物理答案。
- 接管 source frame=1728；第一条模型查询前后保存的仿真状态 SHA 均为 `141c4d8b413227696ff9f21fe940b82e0b67eccdcde82655b02fba8ba7b7082d`；接管后无reset/load_state/演示动作。

## 本人实际看到了什么

本人看过0至95秒、每5秒一张的20面板，以及57.60/62/66/70/78/100秒的6个放大面板；这是关键帧抽查，不冒称逐帧看完全部视频。

1. 演示前缀将机器人带到桌边，收音机就在视野内，不是导航完全没到位的窗口。
2. A阶段，右夹爪靠近收音机，物体呈侧向/竖直姿态。约25–55秒的画面变化很小，主要停留在同一夹持接触附近；仅凭画面不能认定闭合、牢固抓持或接触力正确。
3. G0.5接管初期延续该接触姿态，约66–78秒能看到物体方向变化和手臂调整；末段仍在桌面附近操作。画面不支持“完全不会动”，也不能把物体被拨动等同于稳定抓取。
4. 本窗口未看到此前某些整任务回合那种持续大角度原地转圈。此处主要困难是接触/抓持阶段，不能把所有失败统一归结为底盘旋转。

![全程抽样](/home/wsy/behavior/memlite-results-20260911/C2-radio-train-e121/review_0_95s.jpg)

![接管细看](/home/wsy/behavior/memlite-results-20260911/C2-radio-train-e121/review_takeover_57_100s.jpg)

## 与物理记录交叉核对

本人对全部1280条纠正阶段 `physics.jsonl` 做了连续性/类别统计，并检查首尾及目标证据结构：

- source frames 1729–3008连续，恰好1280个实际控制；没有把同一帧重复查询算成稳定成功。
- 1280/1280为 IN_PROGRESS，理由均为目标未被任一手持有；两手 confirmed/unknown 列表全为空；无 official terminal。
- 最终连续成功帧数=0，门槛为6；`causal_subgoal_success=false`，停止原因 `bounded_budget_exhausted`。
- 底盘指令RMS约为 `[0.03237, 0.01878, 0.000679]`，这只是指令尺度，不是实测位移；与抽样中缺少持续旋转相符。
- 所有记录 `policy_input=false`、`training_admissible=false`。未做物理复放，因为没有成功候选值得申请成功纠正准入；不伪称 replay verified。

本次可确认的是“该实例上，两种控制器在这个接触状态下都没有完成局部抓取”，不能据此推断原始G0.5整体不行、所有实例都不可恢复，或某个单独架构因素就是根因。下一步保留该失败状态，扩展其他训练实例接管，与 A2 的真实闭环动作效果分开比较。
