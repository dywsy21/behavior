# H41：近场姿态选项缺口

负责人Codex，2026-09-22。默认关闭，尚未部署。票见`configs/semantic_robot/h41_near_pose_gap_cpu_block.json`。

H38末帧目标点距夹爪82.97mm。远场转腕的条件是距离大于100mm，而ALIGN只有小于等于80mm才进入；遮挡也会排除远场姿态模式。当前所有改善距离的手臂平移均被原IK拒绝，但保存态原servo加可见深度扫掠可接受10个现有微/小旋转和3个退让动作。此为候选缺口，不是抓取成功证据。

新增显式`--near-pose-gap`。只在原8–10cm区间、单臂PICK、当帧语义接触点已确认、双手实际/命令全开、无持物或历史CLOSE不确定、全部改善距离的已检查手臂平移被拒时，额外检查最多12个现有micro/fine转腕与3个不同方向fine退让。不改步长、IK/碰撞阈值，不排队执行；策略仍只选一个通过检查的动作。

额外选项既经过原servo，也经过实际机器人自体形状与当帧RGB-D的手臂扫掠否决。执行前重新获取RGB-D/机器人关节/自体形状，核对候选身份、状态、夹爪锁存并复查新计划。深度只能否决可见障碍，不能证明未观察空间安全；遮挡不变成安全证明。缺失资格、危险标志或新深度否决时不执行。

公共入口、工程门身份、manifest/result和提示均显式记录开关。默认路径回归530测试通过，SFT174测试通过；合成新增测试11项。197c2ba真实3.11保存态完整控制器23.436s：默认3准入与旧结果精确一致，新增15预检中13通过（10旋转＋3退让），2pitch+仍拒；实际q/锁存未改变，新准入动作身份检查通过。保存态不是执行前新RGB-D，0新物理/模型调用/训练；独立审查待。本修复不解决抓取部位漂移或正确抓姿本身；H38近场多次选宽机身正面而非把手仍是独立问题。
