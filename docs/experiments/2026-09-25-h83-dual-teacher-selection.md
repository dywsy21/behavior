# H83：新来源人工金标上的双模型选择性标注校准

2026-09-25 20:41北京时间；owner Codex，准备中，0新调用/训练/控制。源commit待CPU/独审后固定。预定run `/mnt/nvme_tmp/robodojo_vlm_visual_20260925/h83_dual_teacher_v1`。

## 为什么做、与H82的区别

H82改善到69/81，但N precision37/42没有达到原90%门，且人工发现1个框只圈披萨不圈盘子。该轮三分类全量扩标仍失败，不改阈值、不把N/U合并提分。H83检验**不同模型的一致性筛选能否获得较少但更可靠的监督**，不是重新跑同一81张图。

27B继续固定H82参考图协议提出可见性和框；已训练的H78 2B视觉组件独立用单张原图、不看参考图/teacher答案判断P/N/U。只当两者严格同为P或同为N时，生成“可人工审核候选”；任意U/不一致/格式错误均拒绝自动标注。2B不是独立标注真值，相关错误仍可能共同通过，因此必须在新来源人工金标上验证。框质量仍另经人工审查，一致的P不证明框正确。

## 数据与预冻结金标

- H80第二批10个TRAIN来源组、50态/150图，排除所有47历史训练组和第一次人工审查的10组；与H82七参考组也无交集。父本人已检查全部150图，另6原分辨率复看；生成之前冻结 `configs/vlm_sft/h83_parent_dual_teacher_review_v1.json`，37P/108N/5U。
- core为task0/1/3的90图（37P/48N/5U），分别radio/wastebasket/plate-with-food；task2/4的60图只作radio跨场景负例对照，不能声称已学会蜡烛、罐子等新类别。主质量门只算core，不能靠额外60个负例把准确率抬高。
- 这些来源组后续学生训练释放时整组保留作标签方法开发/视觉验证，不回灌学生训练。仍不称独立最终test；原H80 val/test及H76 val不读、不调prompt。

## 唯一预算与停止条件

- 一次新Git source/run；GPU2≤70GiB、CPU56–59；27B和2B顺序装载/释放，不占0/1/3。2700秒含两权重验证/推理/产物验收，另30秒清理；≤1GiB产物、≥80GiB磁盘余量。
- 150次27B生成（每条≤192新token）、150次2B生成（每条≤32），batch8，各图每模型仅一次，无投票重试；0模型更新、0仿真控制。27B与H82相同30文件/revision；2B与H78相同十文件，adapter固定0080和对应SHA。不是把H78部署到actor。
- 准入必须同时满足：core接受P≥20且precision≥95%；接受N≥20且precision≥95%；core覆盖率≥50%。原始两个模型完整三类混淆也保留，拒绝样本不从原始正确率中消失。U不自动发布。
- 若可见性筛选过，本人再检查所有27B P框及全部不一致/错例；框误差和已知错误逐条记录，不能将修正后样本冒充teacher原正确率。出现系统性框错位或人工框对应正确率<95%不得扩为框监督。无金标IoU不报IoU。
- 若不通过，停止这套选择性自动扩标，不自动追加同配置或降低阈值；转向可信的人工/模拟器像素标签来源。即使通过，也须另固定合格条数、类/来源分布、近重复过滤及五小时真实训练吞吐，不能自动把全部原图称合格训练数据。
