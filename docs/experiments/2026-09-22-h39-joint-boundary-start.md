# H39：真实关节限位和规划余量不能混为一谈

负责人：Codex-parent。2026-09-22 08:51：仅CPU设计/实现登记，尚未部署。H38工程门和SFT原评测源不热改。

## 已观察的问题

第一个heldout i1/base回合在832步固定前缀和12步静止检查后，首个模型动作尚未执行即终止。实际q[16]=1.047196626663208rad，小于机器人标定上限1.0471980571746826rad；距上限约1.43微弧度。它没有越过真实限位，但已进入原规划器预留的0.001rad余量。SafeServo.begin在区分动作前就用缩小的规划区间检查实际状态，因此这类起点可能导致所有动作被拒；Astra正在完成全45token保存态核验。

不能称为模型已经执行了错误抓取，也不能只把margin设为0。原失败要保留，缺失的逐tick实际q不能靠命令轨迹反推。

## 拟议改法

增加默认关闭的明确版本：先验证实际起点在标定硬限位内；规划仍优先使用原来的内缩区间。如果一个关节已经合法地贴近限位，则该次动作的规划区间最多扩展到这个实际起点，不允许继续向该限位逼近。其余关节沿用原余量，碰撞、目标姿态、速率、执行时长和抓取判据不变。

例如，右腕已经贴近上限时，可以保持它不动，让左手动作；也可以寻找向内回退的合法IK路径。不能把状态数值强行裁剪，再声称机器人实际已经退回。

本轮只做独立开关、回归测试和真实保存态重算，预算见`configs/semantic_robot/h39_joint_boundary_cpu_block.json`。如果需要把新执行器接入已训练的120步权重，必须保留原训练来源，显式登记新的共同评测执行器并让base/FT/检索基线一致；不能改旧manifest伪装同版本。新物理回合另行登记，不靠本CPU票追加重置。

## 09:04 CPU实施结果（未部署）

新增`ServoLimits(joint_boundary_start_v1=True)`，默认关闭。合法起点严格在真实硬限位内才准入；实际后续测量沿已有1e-5rad数值容差，输出float32命令另查1e-7rad表示容差，不是修改硬限位。扩展后的规划上下界逐次从该动作实际起点计算，只在已经占用的余量区允许保持/内退；IK、完整关节路径和实际序列化的下一命令均受此约束。反馈记下原硬边界、实际起点与该次规划范围，不能仅凭状态字符串声称成功。

10项针对性测试、完整公共502项（11.691s）、SFT174项（6.342s）通过。覆盖上下限、旧默认不变、内部点计划/命令逐字相同、主动向内与向外、闲置手、夹爪执行、不合法/非有限状态和原碰撞/跟踪门。

实际i1保存态用原47MB完整标定与capture42d0ce…d472核验、4.291s只读重算：旧45个servo准入全拒，新44个通过servo准入且原始q不被裁剪。所有拟定关节路径不越过起点扩展边界，首个序列化命令另核；这不是45个完整RGB-D公共动作均可执行，更不是44次真实动作或抓取成功。报告`artifacts/agentic-vlm-goal-20260918/h39_saved_i1_servo_report.json`。独立代码审查与远端同环境验证待完成。

09:10独审更正：Astra发现f166的紧急safe_hold返回分支绕过新序列化命令检查。在容许的微小实际测量漂移下，这能产生超出规划起点界的HOLD命令。f166未部署；后继将所有候选/HOLD出口统一检查、失败不返回命令，不为紧急分支放宽限位，12针对性测试含8个fallback反例通过。完整回归与独立复审仍待。

09:16最终独审闭合：b991d1c/公共digest fe3a8b49，独立504＋174与真实robo3.11的9反例均通过（0返回越界命令），真实i1旧0/new44准入再核一致；剩余RIGHT_PITCH_PLUS仍被原时长门拒。完整review1c57c78e…b0361已逐字纳Git，保留旧f166缺陷记录。这里只完成默认关core，不授权新runner接入、旧数据身份改写或物理追加。

## 09:37 公共运行入口接入（CPU完成，未部署）

另登记900s CPU、0物理的`h39_runner_cpu_block.json`。公共`run_v2.py`增加默认关闭的`--joint-boundary-start-v1`，必须同时显式启用机器人几何检查与夹爪完成检查；缺少依赖在创建输出/仿真前拒绝。工程门结果必须与请求的边界模式一致，旧门不能替新模式背书，且仍要求整个实现digest完全一致。选项传到唯一ServoLimits构造处，manifest的args及最终result都记录实际选择。

runner三处直接safe_hold均经过b991已统一的序列化命令检查；追加独立测试覆盖退出HOLD的起点范围与真实硬边界，不能因“正在停机”就输出越界命令。17项边界/入口测试、公共509项11.175s和SFT174项5.871s通过。首轮新增测试缺少完整手部标定fixture而报错，改用既有完整标定fixture后通过，未削弱碰撞检查。

本次不改SFT native执行profile，不追认新旧执行器相同，不切换现用H38或i71对照。独立代码审查安排在子FT启动后空档；将来任何物理验证另登记预算并保持base/FT执行器一致。

09:49独审闭合：Astra在原300s票199s完成验证，a3d6c14/digest20a2c54b；独立509＋174和整个工程门复合条件2正8负通过。完整receipt2cf9b3e7…e727已逐字纳Git，runner70653/core5583均父核同SHA。三处safe_hold不绕过边界；越界停机可以拒绝发命令并记录次级错误，不保证任何状态都一定成功发出HOLD。本轮CPU接入完成，仍未部署或产生新物理结果。
