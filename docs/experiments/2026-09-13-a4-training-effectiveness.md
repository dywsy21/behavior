# A4：追加低层训练的实际效果

2026-09-13，负责人Codex / A-02。结论：**出现一次可核验的局部抓取改善；还没有A4完整任务成功率或高低层协同有效性的结论。**

## 训练了什么

从A3-5000完整权重初始化，新增2500次更新；动作专家与VLM LoRA训练，高层B-final冻结。仍用原五任务950/50切分、六帧三相机、32步预测/执行前16步、23个真实控制维度。四卡global batch16、seed29、lr1e-5、新optimizer/scheduler，不是等价断点续训，也不是对“只加步数”的严格单因素实验。

训练在2026-09-13 07:39（北京时间）完成，无墙钟截止；新增40000次样本抽取。完整回读核验2500次Adam计数、504项训练状态变化、冻结模型不变、四rank RNG/抽样状态及数值有限。

- A3 SHA：`865193f1c8a257d72ea159bd7a46cebf945b0fd24905110b831f0e8a3438e940`。
- A4 SHA：`6186704788c27c9fae3502c884df0e259de5242ee8690fe578dcbc1f2632f269`。
- A4权重：`/mnt/sdc1/robodojo/behavior_dev/overnight_a4_20260912/formal/checkpoints/step_2500.pt`。

## 离线变化：有改善，但不普遍

固定80留出窗口、相同manifest/window-set/noise-time sampler。FM由0.198776845081降至0.196943994070，约下降0.922%；不是完整eval。

| task | A3 FM | A4 FM |
| --- | ---: | ---: |
| 0 | 0.142679 | 0.142559 |
| 1 | 0.163655 | 0.158998 |
| 2 | 0.274372 | 0.269659 |
| 3 | 0.161039 | 0.164092 |
| 4 | 0.252139 | 0.249411 |

task3变差，task4的GRASP子组也变差。不能以总均值微降掩盖技能差异；少窗口不作统计显著性声明。

进一步复用原train radio episode121/instance138的42组真实输入、seed17/29，两权重各85次FM生成，合计170次。完整1138模型状态/192 LoRA恢复、42条训练/推理prefix、同seed重复均通过；A3还与此前84份保存的预测逐字节一致。两者都使用正确padding mask和动作0:16，对比原始绝对目标，未把归一化截断后的逆变换当原始控制。

| 正确GRASP条件下的右臂指标 | seed17 A3→A4 | seed29 A3→A4 |
| --- | --- | --- |
| 42窗平均误差（rad） | 0.016484→0.015167 | 0.014974→0.014482 |
| 相对下降 | 7.99% | 3.28% |
| 42窗中误差改善数 | 18/42 | 20/42 |
| 16个明显运动窗中优于保持姿态 | 10/16→10/16 | 10/16→10/16 |

两seed的逐窗误差差值中位数均略微变差，夹爪没有一致改善。因此收益集中在部分动作；此实验也没有错意图干预，不能据此声称低层语义服从已解决。

## 局部物理复测：A3失败，A4成功抓住指定收音机

同原train episode121/instance138、环境重置约定、448条未改原动作前缀、固定正确GRASP、policy seed17、最多1280条模型控制，复用相同物理判据。高层调用为0。

| 项目 | 修正版A3 | A4 |
| --- | --- | --- |
| 模型实际控制数 | 1280 | 464（29×16） |
| 抓取前已满足目标 | 否 | 否 |
| 结果 | 无稳定抓取，80次chunk后均IN_PROGRESS | frame912首次chunk后因果SUCCEEDED |
| 停止原因 | 用完局部预算 | 达到原规则中的物理因果成功 |

A4在frame896仍为`target_not_held_by_any_arm`，frame912确认右手持有指定`radio_89`（评测映射`radio_receiver.n.01_1`），连续10个不同物理帧成立，超过原6帧稳定门槛；`initial_satisfied=false`、`policy_causal_success=true`。不是夹爪闭合或模型自报代替物理成功。

实际审核通过31条事件、29条历史请求、464条模型控制：动作与物理回执逐值一致，模型读到的六帧历史与独立捕获hash一致，成功标签/目标/物理时钟对齐。新服务先通过7次真实wire检查；结束自动关闭本次专用服务，未动旧服务。

两次开始时六帧全部本体字段逐值相同；RGB并不逐像素相同。头/左腕/右腕的平均绝对像素差分别约0.848/0.508/0.563（uint8 0–255），最大差78/27/56。因此不是“所有部署像素也完全相同”的严格复现，仍需跨seed/实例验证。

## 本人人工检查与视频

Codex本人查看A4全程24张抽帧、末尾12帧及720×720最终帧，并对照A3的26张全程抽帧：两者均接近桌上红色收音机，A3后段持续在附近动作而未满足抓取；A4末段右手与收音机形成接触并发生姿态变化，与指定目标持有回执相符。**不能仅凭视频宣称物体已完全离桌、能稳定运输或能完成后续开关任务。** 这次在抓取成立后立即结束，没有检验这些后续能力。

本地视频：`/home/wsy/behavior/artifacts/a4_effectiveness_20260913/A4-radio-e121-L1.mp4`（30.47秒）。源/本地SHA均为`a3dd6da39dc2417f8b591aac09a025c521a32063e4f700e4405984cbaaca7669`。同目录保留抽帧、`A4-L1-analysis.json`和`paired-actions-summary.json`，不入Git。

## 证据与判断

- 训练根：`/mnt/sdc1/robodojo/behavior_dev/overnight_a4_20260912`。
- 配对生成：`/mnt/sdc1/robodojo/behavior_dev/a4_paired_actions_20260913_v1`；配方commit `f9d8937`。
- 局部仿真：`/mnt/sdc1/robodojo/behavior_dev/a4_radio_e121_l1_20260913_v1`；执行commit `c7fb287`，后处理commit `a3ce491`。`actual_analysis.json`为逐条审核，`actual_rollout/physical/e00000030.json`为最终真实物理回执。
- 完整局部结果SHA：`b81afbabb720e3015997375da4a48e00d00076f3463c965a125ff641692e4f35`。旧修正版A3完整五任务仍是0/5，**不能移作A4的成功率**。

现有证据支持“继续更新低层在这个起点带来了有用的控制变化”，不支持“MEM-Lite已普遍有效”“高层已学会完成反馈”或“再多训必然更好”。A4没有改变高层或加入恢复/RL。微小平均loss变化也可能伴随跨过某个接触成功门槛，选模型不能只看loss。

下一步固定A4作为候选低层，不自动再训练：先做少量不同起点/seed的复现，随后少量完整自动高层＋FM闭环，区分低层泛化、意图错误、成功后的阶段切换。反馈头和高层带反馈训练等待可用、审核通过且不泄漏的真实标签；恢复数据扩充与通用RL仍由另外两位队友负责。主仓模型代码尚待独立review/整合，goal未完成。
