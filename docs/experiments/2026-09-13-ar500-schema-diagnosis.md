# A4纯AR500：能学习，但尚未学会完整动作格式

Owner Codex；五任务方法筛选，非50任务正式训练。实际run为`/mnt/sdc1/robodojo/behavior_dev/dual_track_fm_ar_20260913/ar_a4_fulltrain_v2`，代码171898c；A4父权重、新Adam、192个LoRA张量、原950/50切分、global16/seed41/500更新，无FM监督。

## 已完成的训练与效果

500更新、8000原train抽取、完整模型/Adam/四rank RNG保存回读与冻结检查通过。最终权重SHA `51bacc1d9ec1f6d19c7e82e93bed60b8a1eb5d5ffbab5337c9b7ba47a84e90aa`；未实测新进程完整断点恢复。

| step | 固定80 CE | 五task各一条自由生成的完整动作组 |
| --- | --- | --- |
| 0 | 18.6797803 | 0/5 |
| 100 | 14.0474166 | 0/5 |
| 200 | 8.2061711 | 0/5 |
| 300 | 7.1059599 | 0/5 |
| 400 | 6.8592896 | 0/5 |
| 500 | 6.7694657 | 0/5 |

CE下降是可学习性证据，不是动作可用或完整SR提升。最终辅助FM参考0.1976125470，不是AR控制误差；五task最终CE为6.5626134/6.7851502/6.8848691/6.6194772/6.9952188。所有自由输出仍漏组，保护性拒绝，未下发仿真。eval500 SHA `20600c174ae6d7ce454604ba3a5f9c9a31bb1dbce39f130c9ccbfb15ecd7cb6a`。

## 同历史数值诊断排除了什么

原train task0/episode99/frame1782（PLACE_ON）与task2/episode584/frame500（GRASP），不是拿正确技能当自主闭环。分别比较实际完整teacher前向和实际AR缓存循环；仅这个离线检查强制正确历史，独立自由调用不接收答案。

- 原A4：122个下一token中121 argmax一致，位置/类型mask全一致，96个LoRA模块在三路径均调用。
- AR500：122中116 argmax一致，位置/mask全一致；两行full/cached eager CE为6.81763/6.82625、5.30327/5.30684，最大logit差0.25/0.375。并非逐位等价，但两个关键body边界的错误选择在两条路径中一致。
- AR500实际未加权动作token CE为6.81495/5.25977，各60个动作token；EOS单独为7.04036/8.18388。新分项统计已在真实模型前向执行，没有额外优化。

证据：`ar_decode_consistency_a4_v1/result.json` SHA `161649772d3a5b8a519a6f5916b6dd1201d26df1de3bbca2e341e23f4cbea43b`；`ar_decode_consistency_ar500_v1/result.json` SHA `c17363d9f2a7c902d9c12e7804543f73fa6db9a1adba2b8b8b4cc3907f0dd233`。它们不能证明全数据或所有数值实现没有其他问题。

## 更具体的瓶颈：body标记仍极不可能

必须使用真实serializer映射，不能按编号猜组名。已核验grammar的252175/252178分别是**lower_body第0/1残差层**；252185/252186才是左右夹爪。

在完整正确历史下，AR500两行的lower_body第0层标记CE仍约14.91，全词表目标rank约17.1万/15.9万；第1层CE约14.46/17.44。缓存解码也作出相同错误选择。这不是仅仅自由运行时前文逐渐出错，或生成长度用完：模型对这些正确标记本身仍极不支持。

只读检查原生G0.5与AR500的真实输入/输出词表矩阵发现：所有动作标记行逐字未变（当前只训练LoRA）；body两行范数约0.4575，与其他保留未用残差标记接近，而已使用双臂标记约1.58–2.06、夹爪约1.40–1.44；body两行距离仅0.02973128、cosine0.99788970，输入/输出实际共享storage。这提示**近似初始化状态的动作标记＋冻结输入/输出行**可能形成表示瓶颈，但没有预训练日志，不能断言从未训练、数学上不可学习或它是唯一根因。结构化CPU复核已完成：33d739c的`audit_ar_marker_embeddings.py`，`ar_marker_embedding_audit_v1/result.json` SHA `88c4a83e5f11e5df8f967966de1aaa92db2be1ecec1d33b3948c3d7f49778edb`，0VLM/更新/仿真。

## 下一步与不做什么

优先准备“仅开放实际必需动作标记行的共享输入/输出适配＋原LoRA”的有界单变量训练对照，验证对应参数真实梯度、完整回载、零初始化不改变父模型及自由生成；不解冻整个词表、不随机覆盖base、不默认为增加5000步就会解决。19:59已在plan预登记：8个实际marker（16384参数），AR500同父，原十train缓存/五task，两臂各20更新/每次2行，LoRA1e-5、marker1e-3，均eager CE，GPU1串行。只是可学习性筛查，未启动，也不替代原950/50或闭环。

如做纯schema约束解码，必须作为另一推理变体独立报告：只约束静态码块结构，动作码必须仍由模型预测，不能用专家轨迹/归一化零补缺维度；结构完整是人为约束的结果，不是策略质量证明。当前并未实施该变体。

原生task-AR仍按既有有限500预算继续作对照；新CoT已通过输入与两临时更新工程门，但尚未正式训练，先解决这些前置问题。FM五臂队列不变；真正闭环与方法收益、有效组合仍未完成。
