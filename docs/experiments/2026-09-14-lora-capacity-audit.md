# LoRA容量与实际覆盖：只读适用性审计

Codex / M-03-R0，2026-09-14 19:15北京时间。**审计已完成，不是新容量训练或方法收益。** 固定source `e50e7b5d09707060f1a607d7ba2cc3e6ab8b4157`，17 CPU passed（0.12s）；原A4与M04 FM500两个完整checkpoint/SHA及各自配置核实，最多192对A/B低秩分解已用完。0策略前向/反传/optimizer更新/训练样本读取/仿真。原完整结果：`robo:/mnt/sdc1/robodojo/behavior_dev/dual_track_fm_ar_20260913/lora_capacity_a4_fm500_v1/result.json`，SHA `0bb9958df841577b77285c115119f421d73786a567cf8e06f5213e45a370b06e`；[轻量机器摘要](results/2026-09-14-lora-capacity-audit.json)。

## 实际配置和覆盖

两权重对应的实际配置相同：r8、alpha16、dropout0.05、adapter `skill_fm`，不是helper的默认dropout0。实际共有96个适配模块/192个A/B张量。

| 实际投影 | 模块数 | 配有LoRA |
| --- | ---: | ---: |
| MLP的gate/up/down | 各24，共72 | 全部72 |
| 普通注意力q/k/v/o | 各6，共24 | 全部24 |
| 线性注意力in_proj_qkv/in_proj_z/in_proj_a/in_proj_b/out_proj | 各18，共90 | 0 |
| 词表input/output | 各1 | 0 |

这不等于18层完全没参与训练：其输入仍可能被前层及本层MLP适配器改变。但当前target名称列表确实没有直接适配这些线性注意力投影，所以“扩大既有模块rank”和“补充投影覆盖”是两个不同假设，不能混在一轮里改后把收益归给rank。

## 更新矩阵谱

对真实LoRA更新`(alpha/r)·B·A`，用两个瘦矩阵QR后分解8×8中心矩阵，避免物化完整稠密更新。测试与小矩阵直接SVD逐项比较，并验证输入和RNG不变。

- A4→FM500：91/96模块的A和B字节均变化，5模块两者均不变；这5个是最后第23层MLP的gate/up/down及self-attn的q/o。该事实与此前FM只有504个Adam状态、10个LoRA张量无梯度相符；本审计未做新的计算图因果试验，不能据此把它们叫作bug。
- 达到95%更新能量所需秩为8的模块，两权重都仅10/96；另有5个零更新模块。按能量参与率算的有效秩中位数约1.9693→1.9492，更新Frobenius范数中位数0.38559→0.38928。
- 这既不能证明rank8够用，也不能证明已经欠拟合/梯度冲突；不同模块谱不同，不从一个合并中位数推出全网络瓶颈。

## 对后续实现的约束

原A4恢复helper会拒绝直接改r16后的形状不匹配，不可丢掉旧192张量随机重启后仍称相同父权重。若单试r16，必须保持原函数及alpha/r=2、保留已学部分、新增部分初始净更新为零，并独立验证恢复/梯度/推理；只把r8改16且alpha仍16会把原更新缩半。

若单试投影覆盖，rank/已有LoRA/动作专家/数据/日程保持不变，新适配器须零净更新初始化，先证明父函数保持、真实新增参数得到梯度、既有权重完整恢复。不能同时换rank、范围和学习率。19:17选定下一实现门优先覆盖18层的`in_proj_qkv/out_proj`共36模块，暂不扩其他三个门投影；保持r8/alpha16/dropout0.05。**本次尚未实现或启动该训练臂，不默认追加500；先父函数/恢复/真梯度门，原CoT→EMA→尾端日程照旧。**

首次入口把A4配置当作新probe的`formal/config.yaml`，在任何权重分解前失败；实际是原Hydra的`formal/.hydra/config.yaml`。已修明确路径并新增回归，旧配置SHA `4d45b4c2…`完整核实，未修改旧训练目录或重做已经完成的策略实验。
