# H80：面向五小时微调的独立实例数据扩充

**21:13最新：** 原图总42,240仍完整；扣除H83整组保护的10个TRAIN来源后，学生候选为390组/37,440图（各task7,488），其中已登记目标类别的task0/1/3共22,464图，仍不是合格标签条数。仅同来源/同相机dHash贪心诊断，阈值0/4/8/16分别保留37,434/37,358/37,145/36,547；不把感知hash不同称语义唯一，不自动过滤或发布。已本人查看两批各150、共300原图。H82标签门失败，H83新150图金标冻结但v2被新四卡训练占用中断，0预测，当前合格标签批量发布与五小时新协议吞吐/长训练都未完成。详见H83资源/计数JSON；不重复小数据硬凑五小时。

已完成（2026-09-25 19:49北京时间）：固定f303355b5453aaa4937207b619dd938e076a7846，一次1017.244s/exit0；480来源实例、14,080态/42,240原图、9,334,445,089B。TRAIN38,400、validation/test各1920，exact像素全唯一、跨split相交0；并不证明语义无近重复。最终manifest SHA43fe21488f0855985622db846b02991dbbc337316e2583f685fe8076a3aa8bcd。独立终审核清单/来源隔离并抽45图复验；本人分层看完150图/50态/10TRAIN组，记录`configs/vlm_sft/h80_parent_raw_review_v1.json`。**仅原图候选验收，标签未发布、长训练未开始。** H81标注校准质量失败，下一H82参考辅助校准另登记。

2026-09-25 19:03北京时间；owner Codex。用户明确要求更多数据和更长微调，至少准备能支撑五小时训练的数据；H79机器人闭环暂停、不启动。目标是改善agentic VLM的视觉/定位能力，具体是否加入微动作仍待用户选择；动作/接触/完成标签不能从任务进度或历史猜出。

## 规模依据，不靠重复小集凑时间

H78 step10→80的70次真实更新平均1.1360s、batch8，约7.04图/s。五小时估计15,845次更新、126,759次样本曝光。这是旧单图短分类协议的吞吐，不是新定位/长输出实测；新协议先测一小段，随后固定约五小时有效训练和明确总墙钟/步数预算。

现有五任务各200条演示、总1000条，任务0/1/2/3/4帧数429928/1053550/2757612/2739238/2369415（共约935万状态，每态三相机）。不重建50任务数据，不改其他成员的数据/RL工作。现有NVMe可用约1.97TB，sdc1约2.00TB（19:01只读检查），新产物只进NVMe。

原图候选规模：每任务80个TRAIN来源实例×最多32个时间分散状态×三相机，目标约38,400张不同图像；另各8个来源实例作新validation和封存test、每实例最多16态，共至多3,840张。全采集候选上限42,240图、480来源实例；这是标注前候选，合格训练条数必须按去重/质检实际统计，不能把同图多个问题或反复epoch当独立图片。若不足支撑合理遍历次数，先补数据，不能偷放宽质量门。

## 数据隔离与标签准入

20:26 计数澄清：400 TRAIN来源组中有24个登记历史TRAIN复用（task0/1/3分别6/9/9），因此只可称376个相对历史新增TRAIN组，不能称400个全新实例。validation/test各40组与47个历史TRAIN组均相交0；这是原设计允许的训练复用，不是留出泄漏。准备第二批150原图人审时显式排除了全部历史TRAIN以及第一批审图的十组，选中task0 i104/i7、task1 i120/i141、task2 i58/i2、task3 i60/i160、task4 i234/i42；没有读取新val/test来改prompt。

- 固定按来源(task,instance)分组，图像/teacher输出前决定split；原逐task最后10演示对应的5%整实例、H09 val/test、后继开发实例、H76三个视觉val实例全部排除。新val/test还排除所有已知H09/H76/native-state训练实例；新训练可以包含旧训练实例，但不冒充新实例。固定meta/quarantine/review SHA。
- 时间抽样覆盖整段，至少1s间隔，不用演示末帧或技能区间结束推断成功。原quarantine严格排除，保持原720/480像素和解码时间误差≤半帧；记录原视频路径/大小/mtime、frame/PTS、图像SHA。
- 原图采集只输出`REVIEW_PENDING`，不是训练发布。标注teacher先在已人工审过的TRAIN图片上校准与测吞吐；其输出只是伪标签，要做分任务/视角/距离/遮挡/类别/置信度抽查。缺失/矛盾/低置信样本隔离，人工审查只会由父Codex亲自做，保留可定位记录。
- 可见性不等于握持/接触/动作完成；定位必须对应当前可识别目标，不用任务名证明存在，不从特权状态喂actor。盘背看不到食物应保留不确定。部署渲染是PathTracing、原演示不同域，留独立渲染迁移检查，不能将原数据val提升直接当在线收益。
- 模型/标签协议、teacher预算、精确新训练预算在小对照通过后另锁定；尚未准入自动伪标签、大训练或新actor。

## 原图阶段预算

先CPU/独审/干净Git/new worktree/真实身份预检。一次候选提取≤7200s＋30s清理、≤42,240图、≤20GiB新产物、NVMe剩余≥80GiB；CPU48–55（并行解码上限4，每路2线程），0GPU/模型/动作/训练；故不触碰队友0/1。不覆盖既有数据或失败目录，任一来源/hash/时序/隔离失败即拒发布。具体源/run待固定，当前0新采集。

19:23实现：`prepare_expanded_visual.py`严格原图目录/schema/源SHA/PTS/最终timing校验，`launch_expanded_visual.py`一键独占run，以系统GNU timeout执行独立进程组7200s/TERM后10sKILL；即使Python原生解码/线程join阻塞也生效。外部监管最多7230s，只清理自己的子组，exit0和RAW seal缺一不可，失败不自动重试。预定run `/mnt/nvme_tmp/robodojo_vlm_visual_20260925/h80_expanded_raw_v1`；原图只在其`raw/`，监督/日志在外一层。数据可用要求监管completed且RAW验证通过，但仍无标签/训练资格。源码SHA待审查完成固定。
