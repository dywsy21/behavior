# H09Y — second TRAIN reference and minimal GRASP-only experiment

Owner: sole SFT worker. Parent owns harness and physical release. Started
2026-09-19 22:30 BJT: ≤3600 s CPU, first source/profile handoff ≤900 s,
zero reset / neural forward / training in this implementation ticket.

## First independently reviewable change

`native_teacher_group_prepare.py` verifies the already reserved four source
groups, full original arrays and all annotation rows, quarantine and episode
metadata SHA. Only TRAIN task 1 / episode 264 / instance 114 is prepared;
heldout instances 1 and 71 remain excluded. The complete GRASP is source
596..1186, not the 17-frame sample and not the near-grasp prefix 993.
No successful seed or BC label is constructed by this CPU operation.

Explicit `h09y-train114-reference-v1` authorization requires 596 prefix +
590 expert + 12 stability + 1 final hold = 1199 controls; GPU3 only after
parent handoff, 1200 s after reset, 900 s initialization, 80 MiB run,
6144 MiB new NVMe root. Both disks preserve 80 GiB. Old reference budgets
and GPU1 default are unchanged. Initialization checks are cooperative before
and after native reset; they do not asynchronously interrupt a stuck native
constructor. Existing failure accounting and final hold remain unchanged.

The committed authorization template is inactive. CPU source/factory checks
may explicitly inspect an inactive release; the physical runner always uses
the default active-only check. Exact code, prepared bytes, matching executor
and task0/task3 engineering gates remain required in either mode.

Root: `/mnt/nvme_tmp/robodojo_vlm_sft_20260919/h09y_grasp_only`.
Only future physical output `reference_train114_v1` is accepted. Old roots,
failed runs and the successful H09W quarantine are preserved.

The parent-approved staged proposal remains at most one new reference, five
native starts, ≥4 reviewed complete successes across two TRAIN instances
(≥32 macros), fresh 2B 120 updates including the two-update real batch gate,
then two heldout instances × base / adapter / nonvisual NN = six paired
physical episodes. These later stages are ceilings, not current authority.
No heldout teacher/seed/CE data is required for the physical comparison.

Remaining CPU work after the first handoff: whole-run review-bound release
with actual image-byte SHA, forced H09X train/service protocol and shared
public-proprio/history-only execution qualification. No new model effect or
physical success is claimed by this source preparation.
