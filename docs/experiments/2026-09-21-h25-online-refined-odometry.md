# H25：修复后原始起点在线验证

负责人：Codex父；2026-09-21，北京时间。实时状态以`../plan.md`为准。

## 要验证什么

H22能接近收音机，但在98决策/2118控制处因视觉定位质量不足停止，官方成功为false。
H24对全部353段保存图像统一复算，原350段通过变为352段通过，没有新增失败；
这不证明在线成功，也不证明估计位姿是真值。H25检验同一固定前端能否在新回合中
避免这类停止，继续抓取并完成任务，不放宽任何定位或物理成功标准。

本轮还启用已独立工程验证的夹爪命令完成语义。因此H25是两项已审修复的组合验证，
不是随机对照的单因素实验；H24相同输入的逐段比较才隔离定位前端的影响。

## 固定版本与预算

- 源码`a62846238a5dbbb566d697fd93e6ee6ebf5793af`，执行器
  `afd1ec55ab872853c400a1c17f8d52a15076cd590feffc6afd0484e004f89a55`。
- 模型保持原27B，revision`1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0`，
  BF16/SDPA，不启用thinking；431次调用封顶，没有训练更新。
- GPU2；task0/TRAIN138/seed0，专家前缀和保存策略前缀均为0。
- 单次完整回合：192决策、6144控制（包括末尾hold）、reset后7200秒、初始化900秒、
  结果3GiB；整块结果4GiB、自有软件缓存16GiB。无自动重置/重试。
- 保持原joint RGB-D、每6控制测量、全部原支持数/拟合/动作安全门与最多两次SEARCH恢复。
- runtime3.9.1开发环境，不称官方3.9.2盲测。

完整登记为`configs/semantic_robot/h25_refined_odometry_block.json`。
远端结果根：`/mnt/nvme_tmp/robodojo_agentic_20260921/h25_refined_odometry`；
冻结源：`/mnt/sdc1/robodojo/behavior_dev/git_worktrees/semantic_tracking_a628462`。

## 已完成的工程验收

| 工程门 | 决策/控制 | reset后耗时 | 结果 |
| --- | --- | --- | --- |
| task0/TRAIN138 | 24/417 | 313.314秒 | gate_ok |
| task3/TRAIN242 | 24/418 | 262.784秒 | gate_ok |

每门父独立重算全部76段新LK及原joint求解器、144主RGB-D哈希、104实际机器人帧、
22完整运动链、4次底盘消费和4条夹爪真实执行回执；每门本人看7张原始图像，
双端完整视频解码通过。完整本地352,723,286B/319,315,295B，三主文件SHA均与远端相同。
本地路径`artifacts/agentic-vlm-goal-20260918/h25_gates_bundle`。
这些门只证明工程链条可执行，**不算抓取成功或完整任务成功**。

## 在线回合

模型2629456于15:54:57提交，14.498秒载入，部署身份核验一致。
策略2630126于15:56:10提交，15:58:09完成reset；首壁炉原始画面经本人检查，
16:04已实际320控制/第14决策，仍在搜索。此处是运行中检查点，不是最终结果。
`launch_model.json`、`launch_policy.json`和相邻日志保留准确命令与资源身份。

完成后须分别报告官方终止、实际持握/子目标进展、停止原因、调用/控制/耗时，
并核完整视频和物理记录。失败同样保留，不将局部进展或视觉定位通过充当任务成功。
GPU3的Astra数据采集/120更新微调/六次配对局部评测是另一条实验线，不能被本轮替代。
