# H10：独立观察相机与持物参考手解耦

负责人 Codex；分支 `feat/semantic-agent-grounded-20260918`。本块执行预算见 [登记](../../configs/semantic_robot/h10_observer_block.json)，实时状态以 [plan.md](../plan.md) 为准。当前实现完成、物理效果尚待验证；不是成功报告。

## 要改的结构性限制

B20 能确认并保持抓取，但找不到收音机按钮。旧观察接口把“按钮属于右手拿的东西”与“只能动右手、用头部看”绑在一起，20cm持物移动预算耗尽时，仍没有看到按钮。

H10 把三个角色拆开：

| 角色 | 示例 | 含义 |
| --- | --- | --- |
| 目标参考手 | 右手 | 以前观察过的物体表面相对这只手记录；不是按钮真值 |
| 观察相机 | 左腕相机 | 现在从哪个方向、以多大指向误差看过去 |
| 运动手 | 左手 | 转动空闲相机，右手关节与夹持命令保持固定 |

也保留用右手向头部相机展示物体的选择。VLM 仍从实际图像及当前可执行候选里选动作；没有写收音机专用转腕轨迹，也没有读取隐藏按钮位置。MEM-Lite 不是这个修复的前提。

## 约束与证据含义

- 旧持物手路径20cm/旋转60°不变；空闲观察手新增独立20cm/120°账户，所有观察合计最多24次。新增预算如实单列。
- 保平负载不旋转、不松手；另一只手有已知持物、未确认抓取或任何CLOSE后残余接触时，不可当空闲相机手。
- 任意子目标下执行或中断的CLOSE都锁存可能接触；仅显式OPEN完成且当前开口匹配机器人校准的完全张开位置才清除。命令执行结束不是张开/成功真值。
- 当前RGB-D点云与实际机器人link/finger盒对自由手计划做负向扫掠否决。另一只手/负载可见深度不作为自由手自身被过滤。模型响应后重新取深度并核本体稳定，执行前再查一次。
- 上述是采样的可见障碍否决，不是完整运动规划、未知空间安全或持物几何碰撞认证。看不到的地方不能叫“空”。
- 相机转向改善指向不等于看见新物体侧面；FK新姿态不等于新信息。只有下一次真实图像可支持当前按钮可见，官方终止才能认证完整成功。

## 已完成的检查

代码核心 `e73e263`，输入压缩 `225d539`，任意CLOSE接触锁存修复 `465bc8561496c97615f8d48604010f5f5b3555c3`。默认关闭 `--multicamera-inspection`，旧实验不被热更新。

- B20 d4/20 的96个机器人单步IK候选：空闲左腕偏约95°，锚点在相机后方；一个8°转腕可预测减少约7°，未进入画面，更不证明按钮可见。
- 用真实保存深度跑整体新接口，两帧各24个自由手候选通过可见深度否决，11.19秒。单步几何审计不假称完整历史重放；神经静态工具另按原d4起的因果姿态重建路径/尝试账户。
- 465bc85 本地275 harness＋21 SFT CPU通过，含另一手点不被过滤、中间扫掠障碍、实际finger偏移、非pick/中断CLOSE、假OPEN、free回访及独立预算边界。
- 原9图全部保留。精确CPU tokenizer发现初版d4为12280，超过服务12000；只去掉候选里重复的手身份/说明。最终465bc85为11426/9201 token、27383/20895字符，均在原上限内，完整候选/几何审计保留。
- Astra/max发现并拦下非pick CLOSE未知负载P1，465bc85修复后独立复审通过；审查不是物理效果认证。
- 两次真实静态调用均选择左腕roll+8°，九张输入图hash与原观察一致；仍是0控制的接口可用性检查，未获得新视角。

## 第一组真实工程失败与r1修复

465bc85的两个0前缀场景门各24决策/369控制（106.52/109.16秒）失败：reset姿态下左腕roll±8°被原IK/自碰撞约束拒绝，其他20个命令到达，两个可选torso命令也被拒。不得用“无执行异常”替换必须7/8到达的判据。失败目录`gate_radio_h10`和`gate_plates_h10`原样保留。

`5accf6993efddb4533dda55cc1f1cab17554c17c`包含两项修复：

1. **手持目标不能按世界固定目标算底盘接近收益。** 旧84.031mm反例中，底盘前进虚报缩短51.45mm；实际目标随参考手移动，收益应为0。新visible阶段禁止这类底盘接近；用当前可见接触点在参考手内的位置和候选FK计算相对变化，不能拿旧物体锚点当按钮位置。双手共同移动收益0，单独工作手的有效接近仍保留。
2. **工程门检验可行的同幅度动作，不预设某一旋转轴。** 当前实测全张开手上最多枚举6个8°方向，前进及反向均须通过原IK/自碰撞、整段可见深度否决；若全失败仍拒绝。最大64轨迹节点/128含中点采样；模型后fresh执行检查与必达判据不变。两个旧门保存的真实d7姿态均选yaw+8°及返回（48节点/96采样），roll/pitch仍拒绝，0新控制。

r1本地283 harness＋21 SFT、服务器283 harness、Astra/max独立复审通过。23:34（北京时间）只追加两个有限工程reset：`gate_radio_h10_r1`/`gate_plates_h10_r1`在GPU3运行，各24/1536/1200s、0前缀/0模型；还不能报物理通过。执行源5accf69，冻结模型服务仍465bc85/89280/8919，同权重与协议、不热改；两种source身份分开记录。

## r1结果与r2：不能把错误测量当作控制失败

r1两个8°腕部来回都真实到达，4次动作前新深度检查均通过；plates完整门418控制/116.46秒通过。radio在第12个命令结束时失败（201控制/64.93秒）：6cm后退的关节速度积分给出偏航2.072°，刚超过2°门限。

该读数**不是已证实的实际偏航**。早期H08独立探针已证明这种瞬时关节速度积分与机身真实位移不符；策略路径已改为RGB-D测量，但工程门仍使用原始积分提前退出，只检查下一决策开始时视觉数据“有效”，没有用它裁决上一动作。这既可能误拒绝，也可能误放行。

本次只读前帧RGB-D→末视频PnP估计yaw−0.01436°，492匹配/467内点、重投影中位0.261px；末图经过视频压缩，且无动作后深度，因此仅为诊断证据，**不追认旧radio门通过**。

`dfe7c96f552bd182cdeb76d761ca2324f48be403`提取共用`observed_motion_feedback`，让gate在BASE动作后立即读取并保存完整新RGB-D和本体q，与这次动作前帧配对，再用策略端原来的12mm/2°判据裁决。有效性失败停止，没有速度积分兜底；原始记录保留，碰撞/发散/中断不清除。未从不可信关节速度添加yaw纠偏，也未修改机器人配置、物理引擎或控制时钟。

289双端CPU与Astra/max独立复审通过，包含双向误判、硬失败、非法测量以及零控制重复帧不冒充动作位移的检查。23:48两个有限r2工程门开始，仍为各24/1536/1200s、0前缀/0模型，原诊断与完整起点策略预算未增加；物理结果待验。

## r2通过、匹配诊断失败及r3输入修复

r2双门真实通过（radio417/plates418控制，124.660/123.722秒），各22个动作到达，原可选torso拒绝不变。radio后退的双帧RGB-D测量yaw−0.01727°，不是原速度积分的2.072°。完整两门416视频帧、288主观测RGB-D hash及48动作后hash核验通过，旧r1失败仍保留。

随后唯一`radio_h10_matched`在448专家＋362旧策略前缀后执行5动作/96新控制：抓取传感器认证成功、切换寻找按钮，左腕roll+实际转7.860°且右手始终保持radio_89；按钮仍未见。下一次d5动作请求因超过12000输入token被HTTP400拒绝，8成功生成＋1被拒请求，无新动作。完整失败副本36 RGB-D hash、48视频帧核验通过，本人看全部6观察×3相机；不能把局部保载/取景算完整成功，也不重复本诊断。

`5c2b452`/`1d93b29`仅将多相机观察候选的重复字段改为列式表，保留全部选项顺序、索引、负数/false/unknown、原始审计与九张图；不改变安全或动作。292测试及独立审查通过。精确processor证明旧d4=11726（与已保存真实请求一致）、d5重建=12145；新版=9532/10025，明确标注为人工压力的8历史输入=10303/10603。旧B20两个状态实际43/25候选也未超限；这些不是所有未来输入的保证。

原d4/d5两次静态调用18.539秒、0控制，实际token符合CPU，选择都在候选集合内；但d4改选左腕反向转动，指向收益−7.018°，d5则+6.990°。**这里只证明请求可处理，不能说模型决策质量提高**，保留负向选择风险，不追加同帧prompt搜索。

r3采用1d93b2999de782053f6613f41177153b2b75214d，digest428c339abed08da0747abc4412c3c66a41e0695b1a2cb5446821b42c6f37388b；两个新源门各24决策、417/418控制、121.402/122.653秒通过。全部必达项、8次fresh自由手深度检查、8次BASE后有效RGB-D测量已核，旧判据不变。两门已退出后，2026-09-19 00:24北京时间启动原唯一`radio_h10_fullstart`（108260/GPU3），0前缀、96决策/3072控制/2400秒。模型仍是冻结27B、465bc85服务89280/GPU1/8919，从12调用起；两种源码身份明确区分。当前正在初始化，尚无完整成功结果。

## 无前缀起点结果：规划协议失败，不是控制失败

原唯一`radio_h10_fullstart`在reset后、0控制、1模型调用时退出。服务call13用83输出token生成解释文字＋JSON围栏，未触及token上限；strict parser正确拒绝非协议输出。同时JSON内容只有“导航直到看见收音机”，没有抓取/开机，单靠剥除解释也不是充分修复。该次原起点尝试计失败，不能以0控制从尝试分母抹掉；没有完成录像（尚未执行动作），初始三相机图和failure保留，failure SHA`db762b47ae271fd37647bf8d59c471c9cd69b9735d351639bc49d9ddf4355a15`。

旧failure的`EXPERT_PREFIX`是未及时更新的phase标签，不代表执行了前缀。下一H11解决有明确反例的初始规划结构化输出、完整任务提示和失败回执缺失；H10不再次原样运行。新块预算见`h11_structured_planning_block.json`，schema只能约束格式，任务覆盖仍须独立检查。

## 后继验证与验收

先同源2帧静态选择、2个真实控制门，再1条匹配前缀诊断和1条无专家/旧策略前缀的原始起点回合。详见预算；不成功就按真实失败重新决策，不自动复制一串相同试验。

匹配诊断有448专家＋362旧策略控制，只测试接口局部效果，绝不用于完成G-AV1。原始起点回合从官方实例reset开始，完整成功只接受官方任务判据，同时保留所有完整起点失败、实际分母、视频和动作trace。这里使用已经反复诊断的train138开发实例，单例成功也不代表竞赛50任务泛化率。

服务器新证据根：`/mnt/sdc1/robodojo/behavior_dev/agentic_vlm_goal_20260918`；本地：`artifacts/agentic-vlm-goal-20260918`。旧B20/H09源码、权重和失败证据不覆盖。
