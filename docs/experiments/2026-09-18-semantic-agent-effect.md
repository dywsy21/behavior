# H-08：将已修接口推进到真实局部效果

负责人Codex，2026-09-18北京时间；分支`feat/semantic-agent-grounded-20260918`，承接H-07，旧证据不覆盖。用户本次明确授权剩余两卡持续迭代至有效果；每块仍限额、归因和记录，不启动全任务训练或挤占队友资源。

## 第一块：字段兼容版真实闭环

- 假设：H-07已验证的搜索/几何接口，在修正观察契约后能够产生真实搜索、接近和操作进展。若仍失败，按首次因果阻断定位，不用反复相同重跑冒充迭代。
- 固定源e6925d87f90d12a7ab6237cb99daf3279cb3b646；机器人/相机/深度与动作23维保持原契约。原96 CPU在robo已通过。
- 模型固定Qwen3.8-27B revision `1d4bf0f2ff6012fd82039f2fa52739d0dd7c60c0`，不微调、不下载新模型。
- task0 train138/env0/seed0＋448原专家前缀；task3 train242/env0/seed0＋0前缀。前缀不是agent行为，开发实例不是盲测。
- 两工程门各24命令/1536新控制/1200s；通过后每任务一短闭环，各48决策/1536新控制/1200s，服务≤220次生成。仅GPU1/3，保留80GiB磁盘；后续块依据结果登记，不无限无效重试。
- 结果分层：搜索实际新增视野并找到目标；可验证接近进展；正确抓取/操作的时序与物理证据；官方完整成功。前一层不冒充后一层。

## 路径与当前状态

服务器新根`/mnt/sdc1/robodojo/behavior_dev/semantic_agent_effect_20260918`。初始源`/mnt/sdc1/robodojo/behavior_dev/git_worktrees/semantic_grounded_e6925d8`不可热改。本地证据放`artifacts/semantic-agent-effect-20260918/`，不入Git。[实时计划](../plan.md)记各启动/终态与后续块。

两B1工程门已通过（radio385新控制＋448前缀，task3 396新控制）；同e6925d8/implementation digest `028fdacd876bfb13ba3724ae7b1020c3e6f37af6641d2cbd38f25eeaefcc0ab9`。

12:48 B1终态：radio20决策/354新控制（16HOLD、4底盘前进），task3 35决策/834新控制（33左扫、1右扫、1HOLD），两者都未完成首目标。服务82生成；两次恢复输出策略枚举合法，却因`visible_reason`略长于240字符触发异常。修复仅将非执行审计理由上限1024，保留原文，策略与实际控制界不变；两个实际回答回放通过、合计105CPU。B1还揭示radio不同相机误对应、task3局部完整扫视仍看不见桌子；这些感知/导航问题并未被格式修复解决。源/结果/视频归档`b1_complete.tar`，待全段人工审阅。

## 第二块B2：有界表面细定位（准备部署）

通用RGB-D表面细定位：保留原深度边缘拒绝阈值，在原点附近提出最多12个稳定表面点，把原始局部图和单独编号图给VLM，有限语法选择ID或null；不自动吸附最近深度，不读取仿真目标真值，不把选点当持物证明。每回合最多16次额外选择、执行前仍重新走原预检。默认关闭、需独立CPU/静态人工核验与真实控制门，当前B1不包含此修改。保存radio帧已验证候选确实可能含背景，不能省略目标语义选择。

- **主要假设/负责人：** Codex；原2位小数点导致的深度拒绝可由明确的局部表面选择解除，并转化为安全接近。B1为当前同模型同起点对照，其他控制/成功判据不放宽。
- **代码/数据：** `6d18204`，同固定27B revision。旧H-07 radio call4/6两帧，先0控制静态选择最多2调用，本人核查落点；这是相关开发样本，不是感知准确率测试。
- **资源/预算：** GPU1新独立服务8908最多256调用，GPU3旧B1服务照常直到原回合终态；禁止热更新B1。通过CPU与人工静态后，两B2控制门（各24命令/1536控制/1200s）；若过门，再同两task/同实例/seed/前缀各48决策/1536控制/1200s、各最多16额外表面选择。新产物≤5GiB，余盘≥80GiB，0训练/下载。
- **停止/下一步：** 静态选错目标不进策略；控制门失败先定位；无进展记录是定位、可达、候选、接触或状态反馈哪层阻断，再进入另一个有明确变化的块，不原样刷回合。完整成功仍只认官方/物理判据。
- **本地验收：** 104/104 CPU（1.746s），两个真实编号图本人检查；点10在外缘/背景，不能自动选它。`surface_preview_b2`为0神经/0控制预览，实际模型尚待调用。

12:46更新：固定27B实际2次选择都返回ID9，人工确认在红色机身；原边界深度拒绝变为有效点，约2.15mm深度spread，11个可行候选包含右臂forward/up、底盘前进。没有任何执行，不能称抓取改善。`static_surface_b2.tar`两端SHA94f3080d…一致；robo104CPU4.184s。B2源需再纳入上面的恢复理由修复（105CPU），静态模块与服务6d18204字节不变、不重复生成；真正物理收益仍待新门/闭环。

## 独立里程计审计（12:58新增，B2策略暂缓）

`160abd5`两控制门通过，但B1人工图像时序提示“覆盖一圈”的日志可能提前。`audit_visual_odometry.py`仅从既有head RGB-D＋机器人相机标定，用SIFT稳定深度对应/PnP RANSAC估计相邻运动，不读场景真值：34旋转对通过，视觉/速度积分转角比例中位0.791，每次图像估计约6.9°而反馈约8–10°；净221.3° vs286.1°。PnP重投影中位0.2–0.6px。该结果证明两种测量不一致，不能单凭它认定是dt错误或替actor直接乘0.791。

追加一个纯工程probe，原task3起点/GPU3，10命令/320控制/600s、0神经，逐物理步记录实际sim时间、速度和机器人自身的真位移增量（真值仅诊断文件，绝不传actor/保留全局pose）。特殊代码digest不能当正式策略控制门；先确定偏差层级，再修或恢复B2计划。B1策略342次RGB/深度hash均一致；本人审阅18时刻×3视图及radio冲突原图：radio跨视角点位于同一机身不同部位，不能写成已证认错物体。
