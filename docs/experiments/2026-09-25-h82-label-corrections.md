# H82标签修复完成：73条明确监督，8条不确定隔离

2026-09-25 23:02，北京时间；负责人 Codex 主代理。按用户“把这些标签修复完，直至无误”执行。

**这批81张已有标签的已知问题已处理完。** 12处可见性错误修正；32张正例的33个框全部按原图可见范围重标并复核；73条明确的图像定位监督独立导出，8条不能从单图确定的样本保留`uncertain`并隔离。不是把不确定强改成正确答案，也不是宣称所有42,240张RAW都已有合格标签。

## 修复了什么

| task | 已修可见性错误 | 最终存在 / 不存在 / 不确定 | 导出明确监督 |
| --- | ---: | ---: | ---: |
| 0，收音机 | 2 | 13 / 14 / 0 | 27 |
| 1，垃圾桶 | 4 | 12 / 13 / 2 | 25 |
| 3，食物盘 | 6 | 7 / 14 / 6 | 21 |
| 合计 | 12 | 32 / 41 / 8 | 73 |

- **漏认近景**：给两张收音机侧面、两张垃圾桶近景恢复`present`并补框。机器人遮挡、目标超出画面不等于不存在，只框当前可辨认部分。
- **误判确定性**：被饮料罐遮住的垃圾桶候选改为`uncertain`，删除猜测框；三个头部画面底边的器皿碎片也改为`uncertain`，不从过去帧推断其仍有食物。
- **假疑似目标**：把纯机器人外壳、明确空碗内侧等误判的`uncertain`改为图像层面的`absent`。不把它解释为物体已消失或任务已成功。
- **框范围**：并非只修最早发现的一处。收音机把手/侧钮、垃圾桶外壁、餐盘右缘等也重新检查；task3有两只食物盘的画面保留两个框。33框是“全部重标/复核数”，不是宣称原先33框全错。
- **已知披萨错框**：`t3_i96_e678_f006750_right_wrist`原`[0,0,315,488]`仅框食物，现为`[33,0,536,1000]`，覆盖可见食物及承托白盘。坐标是0–1000规范化值，不是480像素原图上的绝对值。

每条保留原teacher目标、冻结人工可见性、当前原图/像素SHA、原像素框、规范化框和修改理由。规范化下界向下取整、上界向上取整；不推断被遮挡的轮廓。轴对齐框内可以包含遮挡物，但不以机械手范围定义目标。

## 实际验收

主代理本人查看原81图的全部九宫格；另44张原分辨率复看（全部32正例＋12个非正例/歧义图）。新标签落盘后，再逐页查看全部9张最终框图，覆盖81张/33框，并对3个关键框做原尺寸复查。8个不确定标签保留，不进入明确P/N训练文件。

自动与独立复核结果：

- 原H76 manifest/gold、H82 predictions和输入协议SHA不变；另固定H83保护清单SHA。81个唯一primary、9个TRAIN来源组、原16条repeat排除；没有27张旧validation或H83十个保护组回灌。
- 原PNG、解码RGB像素、分辨率、来源身份及所有目标协议逐条通过；73/8导出再次独立核对原图路径/SHA和目标，不含旧teacher生成token或错误目标。
- 12项新增目标测试、共27项grounding相关测试通过（整体3.131秒）。独立审查者再次运行12项，并用真实81图重建9页，和主代理审阅的页SHA完全一致；完整临时导出73/8也通过。
- 导出入口首尾强制重验来源、图像及最终图审页；固定同一review SHA；拒绝draft、过期图审、未知/越界框、重复/留出来源和向冻结源包写文件。仅末尾复验通过才生成`release.json`；无封存回执的部分文件不能训练。

**验收边界**：单个Codex主代理逐图修订，不是多人独立标注共识，也没有像素分割金标/IoU证明；“已知问题闭合”不构成数学上的零错误保证。独立代码审查没有冒充第二份视觉标注。

## 哪份文件是正式版本

- Git中的完整修订标签：[h82_parent_corrected_grounding_v1.json](../../configs/vlm_sft/h82_parent_corrected_grounding_v1.json)，SHA `ec82887dcf4157240cc0b9f4d866c80a3880963c4972f369145cee3fee332b2f`。
- 本地正式导出：`/home/wsy/behavior/artifacts/agentic-vlm-goal-20260918/h82_corrected_grounding_release_v2/`。`train.jsonl`73条，`quarantine.jsonl`8条，`release.json`是封存回执。
- 最终图审：`/home/wsy/behavior/artifacts/agentic-vlm-goal-20260918/h82_corrected_review_sheets_v1/`；九页`corrected_00.jpg`至`corrected_08.jpg`，`native_positive/`保存32张原尺寸框图。仅供审查，不能作为训练图像。
- [机器可读验收记录](2026-09-25-h82-label-corrections.json)含12处修正明细、原/新SHA、实际类别统计与导出位置。
- 导出工具：[reviewed_grounding.py](../../scripts/vlm_sft/reviewed_grounding.py)；原始数据仍在H76包，模型输入只能是原始RGB和query，目标采用`visual_grounding.encode`的JSON框协议。现有训练任务不会自动切换到这份文件。
- 加固前`h82_corrected_grounding_release_v1`仅保留追踪，**已被v2替代，不用于训练**。本轮未删除任何旧版本、源图或模型，也没有创建远端训练/仿真进程。

在另一个干净Git checkout重建时，先用工具的`--mode render`绑定原H76包、H82 predictions，输出新框图目录；再`--mode export --overlay-root 新框图目录`导出到全新目录。9页像素SHA必须与已签审记录完全相同，否则拒绝；不要修改review SHA来绕过差异。服务器原数据路径见[SERVER_LAYOUT](../SERVER_LAYOUT.md)，这次尚未在服务器重建导出目录。

## 对后续训练意味着什么

这73条可用于**图像存在性/定位监督**，不能用作动作、抓持、接触、子目标完成或success标签。九个来源组以前已参加H78训练/H81–H82开发校准，不是新增独立实例，不再作为独立测试集。

人工改正不代表teacher变强：H82原模型结果仍为69/81、N precision37/42=88.10%，原失败质量门保持；本轮0新模型生成、0训练更新、0仿真控制。H83标注方法校准与H80大批量标签发布尚未完成，73条不能冒充足够五小时的新训练数据。后续仍需验证批量标签来源，再构造、分层人工复核并核算真正可训练的数据量。
