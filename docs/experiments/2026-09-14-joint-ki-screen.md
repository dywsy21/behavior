# M-04：离散监督与梯度隔离的有限对照

2026-09-14 17:16北京时间，owner Codex。**三臂500训练与全部30次同状态FM生成均已验收；joint/KI相对纯FM均未显示固定80或合并动作误差收益，本轮不升级默认、不扩其radio回合。** 这是一个seed/有限离线首筛的结果，不代表方法族无效或完整任务成功率结论。不重复已完成训练，不追加5000步；高层未参与本轮更新。

## 当前能确认什么

三臂原A4-2500父、新Adam、原950 train/50 eval、四卡global16、seed41、rank8、LoRA/动作专家1e-5、50 warmup/cosine500。部署都仅FM；joint加入离散动作CE且FM可反传VLM，KI加入同一CE但截断FM到VLM的梯度。此处是G0.5 codec的KI-inspired变体，不是PI原FAST配方的完整复现。

三臂四rank全部1000 microbatch，仅将joint/KI的stage路线名替换成`experimental_fm`后，原始字节SHA全部相同，各8000 draw；未对JSON浮点重新序列化。完整SHA、权重检查、曲线与日志统计见[机器摘要](results/2026-09-14-joint-ki-screen.json)。这证明来源/次序一致，**不证明训练增广像素或所有RNG相同**。

| 固定80窗口 | 纯FM | joint（不隔离） | KI（隔离） |
| --- | ---: | ---: | ---: |
| step0 FM | 0.1969439941 | 0.1969439941 | 0.1969439941 |
| step100 FM | 0.1975923335 | 0.1971799073 | 0.1972211104 |
| step200 FM | 0.1969458191 | 0.1971395238 | 0.1971241007 |
| step300 FM | 0.1975663391 | 0.1986530803 | 0.1986480299 |
| step400 FM | 0.1974353622 | 0.1992173228 | 0.1991914165 |
| step500 FM | 0.1975670575 | 0.1994614143 | 0.1995491940 |

joint/KI最终CE分别6.7719212/6.7707832，均从18.6797803下降；但FM比同期纯FM分别高约0.96%/1.00%，KI比joint还高约0.044%。两者除task0略好外，其余四task的最终FM均更差。**本配方没有显示加入CE或隔离FM梯度的固定80收益**；不能据一个seed/500更新否定整个方法族，也不能以两者CE下降代替动作验收。

FM权重`efce4dfe232f85ac18f7fca66b562360ba23d839f3f74748f658b5911b5c33f9`，joint权重`e1a67647570ae0706503625b0891086e1a15474e335177aed6b44a35553b9fd5`；实际Adam状态数504/514，冻结参数及完整模型/优化器/四rank RNG保存回读通过，但未实测新进程续训。三臂实际声明可训练参数名集合均相同（514项），不能把Adam状态数差异直接解读为多解冻十项；未据此增加/修改参数范围。旧M-01的FM500不是本表同入口对照，不互换它们的动作结果。

## 新发现：全局裁剪仍把两个模块耦合在一起

KI于17:04:46正式complete，完整514 Adam/冻结及保存回读通过，权重`3efc6d1ab77cd02b01fa0a3db92262d522636fa2085e107dca04d7f3163e45ea`；eval/inspection SHA分别`029a00f98223202517e52376858bc02b1bedee308197c8af2c5e26fcdf816fc4`/`9cbdf1cb299b3259802da145a15e72e76acc84ba377038e7ce30f022f9bde96f`。同样未实测新进程续训。

16:40补充实际反传回执：两臂均声明322项AE＋192项LoRA可训练；rank0中FM实际有梯度322 AE＋182 LoRA，joint是322 AE＋192 LoRA（其中187项非零）。与504/514 Adam状态数相符，差异来自离散监督使更多LoRA参数连到损失，**不是多解冻十项动作专家**；回执在各run的`formal/coordination_grad_receipt_rank0.json`。此计数仍不测梯度夹角或各loss范数。

真实训练代码`train_action_method_probe.py`在两个microbatch反传后，对所有可训练参数一起调用阈值1的梯度裁剪，再执行AdamW。日志的`rank0_grad_norm`是裁剪前总范数；本次只读统计，没有改现有训练。

| 原日志范围 | 触发裁剪的更新数 | 总梯度范数中位数 | 平均裁剪系数 |
| --- | ---: | ---: | ---: |
| 纯FM 1–500 | 3/500 | 0.2762 | 0.9991 |
| joint 1–500 | 500/500 | 2.2018 | 0.3903 |
| joint 1–100 | 100/100 | 11.6956 | 0.08574 |
| KI 1–500 | 500/500 | 2.1964 | 0.3910 |
| KI 1–100 | 100/100 | 11.7187 | 0.08575 |

因此KI的FM→VLM计算图虽隔离，动作专家的梯度缩放系数仍受其他模块梯度影响；最初100步在两种双监督配方中，所有梯度平均只保留约8.6%。**这是代码与已有日志支持的优化耦合事实，不是根因证明，也不能说动作专家学习率变成8.6%：AdamW的动量、二阶矩和衰减不会如此简单等价。** 还没测每个loss的梯度方向、分模块范数或真实参数更新比例，不声称已经证明梯度冲突或“CE压死FM”。先完成M-04已有证据；是否做单项分模块裁剪对照，须另外登记有限预算，当前未启动。

表中系数按裁剪前日志范数与原clip公式复算，不是独立记录的参数更新倍率；两条双监督臂全500的均值接近，但不足以说明分模块裁剪一定有收益。

## 同状态连续动作验收

16:40工程跟进：独立dc2fe36的`action_gradient_clipping.py`提供显式global/per_module方式，真实PyTorch2.7.1下44 CPU检查通过（0.18s）；global保持原参数顺序/梯度/RNG及玩具AdamW状态，按真实模块校验覆盖，不按机器人动作维度裁剪。[实际命令、SHA和验收边界](results/2026-09-14-module-clipping-cpu.json)。per_module各组上限1，所以合并范数可至sqrt(2)，不是保持总范数1的新名字。**未接入trainer、未做真实GPU-DDP训练或方法对照，不改本表任何权重/原运行，也不把单元测试当收益。** 是否测新配方，仍等KI完整结果再有限预登记。

现有44255a4/161 CPU入口，每臂仅原5 train＋5 heldout窗口、seed17、10次FM生成，三臂总30上限，0 AR/codec/更新/仿真。真实1138模型状态和192 LoRA项逐位恢复；目标在actor外评分，23维控制、32预测/执行0:16不变。按有效scalar合并误差，短尾padding不记零误差，不平均窗口RMSE。

纯FM result SHA `d3502794bb1d3ff7c27d8838c55c331a8faba22b23dd32ff82c4fdf2d87193c7`；joint `3e1f5eb6b3c2f9437aae4083c1a0d56fb0f8c647fad6aa032eabdb68de706dec`；KI `8e5434b647bdf40117bf16abf5b950508941d9f85805c3a9b7e0c54c6ca6c29c`。三个动作进程均已退出，三臂完整恢复均通过，**十窗来源、实际三路图像/六帧本体/mask张量指纹以及A4缓存目标评分全部一致**；KI已分别与FM和joint逐项比较。这是本次推理输入实测配对，不仅依靠同seed假设。

| 归一化动作RMSE | 有效scalar | 纯FM500 | joint500 | KI500 |
| --- | ---: | ---: | ---: | ---: |
| 五train执行0:16 | 1403 | 0.4470648 | 0.4525489 | 0.4525651 |
| 五heldout执行0:16 | 1840 | 0.8818891 | 0.9047233 | 0.9060167 |
| 五train后段16:32 | 1104 | 0.6300357 | 0.6345728 | 0.6345149 |
| 五heldout后段16:32 | 1840 | 0.9744828 | 0.9808204 | 0.9813625 |

joint/KI相对FM的heldout前段误差分别高2.59%/2.74%，train前段都约高1.23%；两split后段也更差。两者均只在相同4/10窗更好（train task2/4、heldout task0/1）。KI相对joint有5/10窗更好，但heldout前段合并误差仍高约0.14%，没有逆转相对纯FM的退化。

**本轮判定：** 两种双监督配方均不升级为默认，不因为CE下降而追加5000或扩跑radio；梯度隔离不能挽回本次有限配方的离线表现。原30生成预算已完整用完，0 AR/codec/训练更新/仿真，不再重做；KI的1547266已退出。全局裁剪有可验证的耦合，分模块裁剪只有CPU实现验证，因果收益仍需单独、有界的对照；不靠这个未测解释替现有配方“翻案”。

**未完成/限制：** M-04本轮没有闭环或完整任务成功率证据，固定监督技能的十窗不等于高低层自主协同；原未见起点、种子重复与50任务泛化未测。M-03容量/EMA/日程、原AR marker/CoT与兼容组合的全目标仍未完成，不因为本首筛结束而关闭goal，也不把三条纯FM部署臂当AR实验。

根目录：`robo:/mnt/sdc1/robodojo/behavior_dev/dual_track_fm_ar_20260913`。训练run为`fm_action_control_v3`、`joint_a4_fulltrain_v3`、`ki_a4_fulltrain_v3`；动作run为`m04_actions_{fm,joint,ki}_v1`。活跃8fcf61f/9f26b45/d114581/44255a4源不热改，其他AR队列不重启。
