# H85：从官方演示构造完整动作监督

2026-09-26；负责人 Codex 主代理。目标是准备大量可用于 VLM 动作微调的数据，最终支持至少三小时的有效训练。本文记录动作数据，不把 H80 视觉图片或物体框标注当成动作监督。

## 已完成：旧动作接口的真实产率审计

源码 `9f9aee68fe85c8b15eb34a02f74d83b8215a87c8`，五任务各四个合法 TRAIN 实例，共 183,288 帧。CPU 18.669 秒、exit 0；10 个目标测试和独立代码审查通过。服务器结果：

`/mnt/nvme_tmp/robodojo_vlm_actions_20260926/h85_capacity_v2/audit/result.json`

本地：`artifacts/agentic-vlm-goal-20260918/h85_action_data_v1/capacity_v2_result.json`。双端 SHA256：`f8fd2e8514749be09180a330387bcd5e4a210fdc2a6ebcb5fe7f5b32758a80ba`。

| task | 同技能16帧窗口 | 旧方向候选 | 其中底盘 | 其中操作 |
| --- | ---: | ---: | ---: | ---: |
| 0 | 401 | 46 | 44 | 2 |
| 1 | 1,133 | 33 | 32 | 1 |
| 2 | 3,241 | 273 | 263 | 10 |
| 3 | 3,209 | 369 | 352 | 17 |
| 4 | 2,856 | 225 | 199 | 26 |
| 合计 | 10,840 | 946 | 890 | 56 |

946/10,840=8.73% 可以按旧保守规则归成一个方向，其中94.08%是底盘。36个手臂位移/旋转候选，仅6个端点落在当前固定幅度的必要容差内，30个不匹配；其余20个是夹爪开合命令。必要端点容差不是原生执行等价证明，夹爪命令更不是抓取成功。

主要拒收：底盘混合/变速5727、躯干不可表示1784、手臂混合/曲线884、非同向双臂771。该结果否决“沿用旧单方向标签直接扩量”的数据配方，不是否决 VLM 路线，也不是完整任务成功率。

## 已构造：完整动作中间集（尚非最终SFT发布）

固定源码 `8e179837581bd6698b84f574e4fd789b2ef72f62`，真实构造305.689秒、exit 0、466分片、418,873,059字节。服务器：`/mnt/nvme_tmp/robodojo_vlm_actions_20260926/h85_native23_corpus_v1`。manifest SHA256：`851b3cd9709f5dc9db2b378078ece93e9cc1f625278f0afa05994db5e82787e4`。

| task | TRAIN动作窗口 | validation | test |
| --- | ---: | ---: | ---: |
| 0 | 9,422 | 942 | 893 |
| 1 | 23,613 | 2,396 | 1,930 |
| 2 | 62,726 | 6,817 | 6,164 |
| 3 | 63,027 | 6,994 | 6,600 |
| 4 | 53,712 | 5,860 | 5,118 |
| 合计 | **212,500** | **23,009** | **20,705** |

16:54:06北京时间独立逐分片重算全466 SHA、256,214个唯一ID、所有行来源/分组及三相机时间引用，通过且进程已退出。完整manifest及回执在本地`artifacts/agentic-vlm-goal-20260918/h85_action_data_v1/corpus_v1_{manifest,independent_qa}.json`。像素实际解码与人工抽查随后完成，范围和限制见下节，不能据此宣称全量视觉无误。

保留既有 H80 按(task, instance)分组的来源和所有旧保护，排除 H83/H84 的14个整组校准实例。最多386 TRAIN、40 validation、40 test；不引入public_test，不改原逐task 5%留出。窗口长16个30Hz控制步、步长16，不把重叠滑窗重复计作独立样本。

每个样本包括：

- 当前时刻三路 RGB 的原视频、精确episode偏移和帧号引用；不是重新渲染或生成图。
- 当前 `observation_state[61]`，以及同状态下的任务与实际技能说明。
- 原演示真正执行的 `expert_action[16,23]`，完整保留底盘、躯干、双臂、双夹爪。
- 私有审计索引：task/instance/episode/frame、源数组与输出文件 SHA、隔离和来源记录。索引不能直接成为模型提示词。

23D沿用现有R1Pro native控制布局：0–2为归一化底盘速度命令；3–6为躯干关节目标；7–13左臂关节目标；14左夹爪命令；15–21右臂关节目标；22右夹爪命令。不是27D padded动作，补齐位不得套用到这里。未来时间的信息只在监督目标/离线审计侧，当前输入不含未来状态或成功判据。

构造器逐条核对 Parquet 往返后的61D当前状态和16×23动作与原float32数组完全一致。RGB此阶段只校验路径、视频规格和时间引用，后续必须实际解码验证；不能凭索引存在声称图片已验收。

train/validation/test已物理分目录；每行重复绑定来源split、task/instance/episode和三条视频/时间引用，避免靠全目录glob混入留出集。冻结H80分组清单与labels/quarantine/episode元数据的身份链显式一致。过滤前窗口上限为226,420 TRAIN、24,279 validation、21,880 test；剔除技能边界、隔离和未绑定指令后为上表实测。16个相关CPU测试及独立代码审查通过。

CPU预算：1800秒、4 worker/8核、最多350,000窗口/20GiB、保留至少80GiB空余。0模型、0训练、0仿真控制，不操作队友任务。输出仅新NVMe目录；原数据与已完成审计目录保留。

## 已完成：50实例动作—图像人工抽查

固定导出源码`a64896ea594a3548389c84d936cdff3926e41407`，CPU53.130秒、exit0；20个相关测试及独立代码审查通过。每task取10个不同TRAIN实例，分别覆盖底盘、躯干、左臂、右臂、双臂、夹爪目标切换、混合动作及早/中/晚区间；50例均实际找到目标分层，无fallback。每例原视频解码t、t+8、t+16的三相机，共450原PNG与50张仅供人工查看的拼图。未来帧/状态只作离线审查，不能进入actor输入。

服务器原件`/mnt/nvme_tmp/robodojo_vlm_actions_20260926/h85_action_review_v1`（108,315,110字节），本地完整manifest与50人审图`artifacts/agentic-vlm-goal-20260918/h85_action_review_v1`（13,932,316字节，另有transfer回执）。manifest SHA256：`19900e355372542fbe3504692980fcaf41b58dee75ae4f2544bbd98cbf2447bc`。

主代理本人17:17完成全部50人审图查看，逐例记录`configs/vlm_sft/h85_parent_action_review_v1.json`，SHA256：`ca5401b5b789f4d880267608dbb22810cfd48fb2eb85767bb03d87b5e21ccb12`。复核50唯一ID/实例、每task10/每分层5、全部实体拼图SHA、TRAIN-only/450引用通过；独立审查者另核上述项目、图像解码、三视角时间容差及发布限制通过。原450 PNG不在本地包，不把本地回执核对写成再次逐张原PNG核验。保留旧导出manifest的PENDING历史，由新回执关联，不修改封存产物。

抽样中未发现已确认的场景/相机错配或明显时序跳变，但这是每格320px的样本级视觉检查，不是全部原尺寸图像的细粒度标注，更不是全256,214窗口零误差证明。明确发现并记录以下真实数据特征：

- 腕相机常被机器人身体或所持物体遮挡；不能据此当作丢帧，也不能假设单腕图能识别目标。
- 同一段动作经常同时含底盘、躯干、双臂与夹爪，不能压成一个方向而保留原意。
- NAVIGATE可能含展开双臂或原地调整姿态，GRASP含接近阶段，PLACEIN/CLOSE含收手或静止收尾。**技能文本是区间意图，不是即时动作或完成真值。**
- 开合命令、物体看似被持有或最后一帧看似就位，都不产生官方成功标签。

## 来源说明：与原42,240张视觉图片的关系

H80图片和本动作中间集都来自服务器已有官方演示，而非本轮新运行模型/模拟器采集。原根为`/mnt/sdc1/xhz/BEHAVIOR2026/2026-challenge-demos`，五任务转换/别名入口为`/mnt/sdc1/robodojo/datasets/behavior2026_g05_tasks_0_4`。H80从480个来源实例选14,080个时刻，每时刻头部/左右腕三相机，得到42,240图；保持原720×720/480×480像素，记录视频时间戳，非生成图。

H80后来补的可见性/物体框属于视觉监督；原演示已经包含61D机器人状态和实际23D动作。H85重新沿合法实例取更密的16步动作窗口，完整保留原控制并绑定当前三RGB索引，不是让27B凭静态图猜关节动作。两者图片/样本计数不同，不能把42,240图说成42,240条独立轨迹，或把256,214窗口说成同等数量的新采集演示。

## 尚未完成，不能以中间集代替目标

1. 完整动作到最终 VLM 输出/执行接口的协议尚未冻结。旧单方向不适用；组合动作需要连同执行端对齐。原23D保存为可信底稿，不意味着已批准把agentic接口替换成数值VLA。
2. 全量构造与独立来源/隔离复验、50窗口实际RGB解码与本人抽查已完成；全量训练时的RGB加载/时序校验尚未完成。抽查不替代最终输出协议的数值往返/执行等价检查。
3. 训练加载器、assistant-only监督、token长度/截断检查，以及按实际吞吐量制定至少三小时有效微调方案。不能靠重复极小数据集或用旧视觉分类吞吐冒充。
4. 最终监督发布必须另行验收；当前始终`training_eligible:false`，不据中间Parquet数量宣称goal完成。
