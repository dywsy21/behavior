# H17：先保留换位候选，再做安全预检

负责人 Codex-parent；Astra 独立终审。2026-09-19 北京时间。仅方法诊断，仍是 v3.9.1 开发环境，不是正式竞赛成绩。

## 最新结果：已完成，有接近改善，尚无抓取

固定34114892/b2d14390，双工程门通过并退出，父核288主RGB-D hash、44完整动作链/152段/304端点和835连续控制。随后唯一1392保存策略前缀局部回合完成：10动作、180动作控制＋1安全hold、20模型调用、481.014秒。六次fine转向、三次fine侧移、一次右臂fine前伸，全部TARGET_REACHED，但**没有CLOSE、没有抓取、官方成功false**。

为避免VLM重选像素造成误判，下表固定首次观察到的同一表面点，用完整机载RGB-D运动链和机器人FK计算距离。假设未接触的收音机保持原位；这不是特权对象真值，也不是抓取指标。

| 同起点前10完整动作 | 距离起点→终点（m） | 缩短（mm） | 动作控制数 |
| --- | --- | --- | --- |
| H15原候选 | .682246→.614416 | 67.830 | 191 |
| H16调腕 | .682246→.593063 | 89.183 | 216 |
| H17底盘候选 | .682246→.558302 | 123.944 | 180 |

H16/H17清空了旧历史，候选版本不同，不能当严格固定策略A/B或混算完整SR。H17全10步候选预检共177.723秒；H16同10步376.902秒，因此仅调腕的两步前瞻成本较高。H18的等价FK加速另有独立数值/服务器证据，不倒填到H17计时。

H17前6次转向实测累计yaw .235294rad，终态相对初态平移约[-.04266,.17011,.00672]m、yaw .231951rad。动作原始feedback中的base_integral是关节速度积分，不作为实测；正式运动链首转向为.039067rad，不是原始积分.068916rad。

完整147,454,991B已本地`artifacts/agentic-vlm-goal-20260918/h17_matched_bundle/radio_h17_matched_approach`：3主SHA、整视频解码、60主hash/10链30段60端点FK/9次BASE链消费/181连续控制全部通过，父亲看末头图与最后动作前右腕，收音机仍在桌上。准确SHA及预算结果在`configs/semantic_robot/h17_body_options_block.json`。下一H19另登记联合腕姿＋底盘＋等价FK后的零前缀原起点回合，不追加H17。

## 依据与改动

H16 1392 保存策略控制后的局部实验已结束：10完整动作/216控制，另1中断控制和1 hold，共218；22模型调用，660.561s。600s预算实际超60.561s，不能追认合规。头图仍见收音机在桌上，无close/抓取。完整副本 `artifacts/agentic-vlm-goal-20260918/h16_matched_bundle`，66主RGB-D hash、218连续控制、视频完整解码及三主文件双端SHA通过。

前10完整动作距离变化：H15同起点 .682246→.614708m，H16 .682246→.593192m。后者多接近21.516mm，但控制191→216、候选163.407→376.902s；观察表面点距离不是抓取真值，而且H16清空了旧历史，不是严格策略A/B。调腕接口确有执行证据，昂贵两步前瞻暂不带入本对照。

另一个已定位错误是：原palette包含六种底盘方向，但目标可见后只挑一个预测最短距离的平移，再做预检。这个平移若被挡，其他方向和转向已经消失。H15 d63/77/89的12个fine/micro转向在原depth/servo下都通过，却从未提供给VLM。

H17增加可选 `--approach-body-options`，仅在明确空手、双爪实际全开且命令也为开、无接触/持物未知、可见无危险、距离>10cm的单手PICK/APPROACH时，保留全部六向fine底盘候选。单方向拒绝后才试既有micro，最多32个当前非hold预检。所有depth、机器人几何、限位、实测进度否决保留，不修改动作幅度，不自动执行转向，不加入特权状态或逐任务动作脚本。H17不打开 `approach_reorientation`，从H15接口单独测试底盘覆盖，而非把所有新选项一并开启。

同一三保存态的真实生产采样已过：24/24/25个非hold预检，总14.247s本地CPU；每态两向fine yaw进入合法选项，d63/77还保留right fine。各候选5.611/3.389/3.950s，不能据本地速度直接承诺服务器物理吞吐。输入状态和原servo未变；原始记录 `artifacts/agentic-vlm-goal-20260918/h17_body_production_candidates.json`。

## 时间边界

新增协作式墙钟检查：模型调用前、图像编码后、HTTP等待（用剩余时长）、候选每次预检前后、选动作后和IK/传感再检查后、每个控制量子前。到期不再开始下一动作。不会用异步异常打断物理引擎；已开始的单个CPU/控制量子、记录和最终安全hold可跨边界，实际耗时仍如实报告。

部分执行的里程计仍标为不完整，但原因明确记录为时间预算，不能伪装传感器失败。最终hold现在使用最后**已发出**的夹爪命令，防止 `servo.begin()` 已修改目标开合、但动作因到时未执行，收尾却悄悄执行新开合。

本地354 harness/6.384s、60 SFT/.226s通过，含真实runner开动前/后检查、编码耗尽预算不发HTTP、取消开合后保持旧命令、候选六方向与回退/旧路径/未知状态否决。独立终审、远端CPU和新同源工程门尚待，不能把代码测试称为物理效果。

## 原有限物理预登记（现已完成，保留原约束）

精确配置见 `configs/semantic_robot/h17_body_options_block.json`。新输出仅放独立NVMe新目录，累计≤1GiB；H16及之前7GiB累计旧证据保留原盘，不迁移、不删除、不抹预算/失败。GPU3父、GPU1子、GPU0队友不动。

独立终审和新固定源CPU通过后，先task0/138与task3/242两个工程门（各24决策/1536控制/1200s，0前缀模型）。门通过且图像/FK/全运动链审核后，只跑一次H15 d63的1392保存策略控制前缀，再最多10新决策/384含hold控制/600s/43调用。模型仍原27B固定权重，daemon原170/215预算若不足则不启动。与H15/H16前10完整动作比较真实目标距离、底盘实际运动、抓取与耗时；不计原起点SR。失败无自动重试，未见有用改善不追加原样回合。
