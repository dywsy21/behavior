# H22：让环境里程计排除机器人自身

负责人：Codex父；Astra独立终审。2026-09-21 11:51 BJT登记≤1800s CPU块。
唯一假设：头相机中机身/手臂随机器人运动，违反里程计的静止环境假设；
在拟合前以当前机器人几何排除这些点，可以减少错误位移和非必要停机。
原起点官方成功仍是goal；本块本身没有新reset、模型或训练许可。

## 证据与范围

H21原起点0专家/保存策略前缀，在98决策、2118控制后因视觉质量门停止，
未闭爪/未完整成功。末4动作11段基线计数和状态精确重现。
失败2111→2117段排除16个原始自身匹配后，从32/92内点变48/77；
双向中位投影误差1.013/1.013px变.224/.219px，深度误差7.744mm变3.224mm。
全部旧质量门不变，其余10段仍通过；3.497s只读诊断。
这证明此保存数据中的污染，不证明新闭环已经越过故障、模型会抓取或任务成功。

## 实现

- 新`--odometry-self-exclusion`缺省关闭，只允许显式六控制步的joint RGB-D路径。
- 沿用同一SIFT匹配和深度可靠性检查。每个匹配的两个3D端点各转换到该时刻
  机器人底座坐标；任一端落在机器人视觉link盒内，就在求解前排除。
  不按拟合残差挑遮罩、不混试求解器，不改变25内点/.45比例等门槛。
- 只使用当前机器人自己的视觉资产与关节/手指FK；没有场景分割、物体真值、
  世界位姿或任务结果输入。link集合/静态bounds/边距必须与标定资产一致。
- 每次采样重新取实际native link变换，包含闭爪后的真实手指位置；采集前后
  q/夹爪不许变化。帧同时绑定q、夹爪、控制钟、RGB-D内容，缺失或陈旧就拒绝。
- 每个六步片段和完整动作证书带几何帧hash。动作结束的精确图像、状态和几何
  一起复用，不能用下一帧的几何冒充原测量。SEARCH新局部参考也继承过滤选项；
  不扩大原搜索恢复资格、不把未知位移补写为有效。
- 全部被排除或环境纹理不足仍停止。机器人视觉盒是保守近似，可能误排紧贴的
  环境点；不能把通过里程计当无碰撞证明。持物自身运动和其他动态物体仍不是
  静态世界，当前修改不宣称解决全部动态场景。

保存态审计脚本对整轮所有实际片段固定重算旧/新路径，不挑通过的片段；
只有开手且实际开度不变时才允许离线搬运旧robot-only盒。**这种搬运仅用于
诊断，部署实际取每帧手指FK**。原图像/深度hash与FK必须同原链。

## 当前验收

12:01 BJT：392 CPU回归/9.370s通过，其中11组新增测试包括双端光学坐标转换、
完整资产覆盖/边界、陈旧q/夹爪/控制/RGB-D、全自身点无虚假支持、原路径不变、
结束快照严格消费、SEARCH重建两次factory均继承选项、实际runner几何采集块。
H21整轮保存态回归与完整证据审计正在同一有限CPU块执行。
固定SHA独立审、真实远端CPU以及另登记同源工程门通过前不启动新物理。

12:05 BJT：固定d7c8028的整轮356段保存输入重算结束，176.570s，原355/356、
新356/356，0退化/1恢复。全部原段的hash/FK与基线计数/状态一致。
原H21全证据另审588主RGB-D hash/97整链＋1失败/712FK/65整BASE消费、
2118连续控制及全视频解码，18.279s通过。Astra独立只读review进行中，
未启动新父物理；输入轨迹不变的离线回放不能证明下一步交互或完整成功率。

12:24 BJT：Astra独立固定d7核心审/392测试7.680s/全356保存段184.279s通过，
同样355→356、0退化，无阻塞。远端同源392/18.749s、模型依赖CPU8.130s过。
12:27另登记物理块`configs/semantic_robot/h22_self_odometry_block.json`：GPU2两个
顺序0前缀工程门（24决策、1536控制、1200秒、384MiB/条）后，条件唯一原起点
task0回合（192/6144/7200s/同27B431calls）。新结果4GiB、独立NVMe缓存16GiB；
脚本/阈值/旧H21证据不改，不与子SFT的GPU3和Kit appdata混用。
12:28:38首门2573658真实启动，12:37已至少82控制/3动作，无故障；首head本人
确认原壁炉起点。尚未完成两门或新VLM闭环，不把工程动作当方法任务成功。

13:14 BJT更新：上述两门已全部完成并完整本地归档。task0为24决策/417控制/
428.596s，task3为24/418/256.467s，均gate_ok；各自144主RGB-D hash、104同帧
机器人几何、76片段/22整链/4次完整BASE消费、连续控制和视频双端独立核过。
父亲看每门首head与两手开闭RAW。两门中自身点排除实际都是0，所以它们验证
接线和回归，并非真实闭环故障场景；有排除收益的证据仍是原356保存片段。
详细SHA/时长/容量见固定实验配置，完整证据本地`artifacts/agentic-vlm-goal-20260918/h22_gates_bundle`。

13:13:14.549421同d7模型2588439在GPU2/8926启动，19.307s加载；13:14:06.223157
同源唯一原起点2589106启动，0专家/保存策略前缀，仍192/6144/7200s/431调用。
13:16 reset及初始校准完成，父首head确认原起点；运行未结束、尚无官方成功。
服务为同步HTTP：父一次并行健康GET在首plan期间5s超时，随后服务写回该已关闭
诊断连接留下BrokenPipe；真实策略POST继续正常、未为此重启或再计一条试验。
以后活跃生成时读调用账本/进程，不用短GET超时认定模型故障。线上源保持不变。

## 实际终态与下一项有限验证（14:38 BJT）

新回合仍失败：98决策/2118控制/164模型调用/3565.854s，两种前缀均0，
官方false、未闭爪。不能把旧保存态356/356通过泛化为新在线修复成功。
完整远端1,145,789,002B由父31.120s独核：588主RGB-D hash、453同帧绑定、
349动作段＋4恢复段、95有效动作链＋2恢复链、64次BASE整链消费和2118连续
控制；3失败原样保留，18/19决策的两次12控制恢复都有完整实测证据。
服务164调用和identity已本地双SHA核验后退出，GPU2释放。完整策略下载尚未
全部结束；不能把局部文件或目录存在写成完整本地归档。

末2111→2117确实排除25自身匹配，剩78唯一点；70个固定3D支持点中45满足
联合约束，双向中位投影1.028416/1.020471px，故原1px门停止。相较上轮，
这是**同一个停止位置的新测量失败**，不是过滤选项漏接。
父完整原图及所有70支持点误差图显示：近景地板、沙发、台灯也有偏差，不能
仅归因于手臂或目标被推动。旧3D拟合在同70支持点近景中位2.857px、远景
.398px；PnP在自身筛出的56点为.848px，但同70点仍1.013px且3D误差变大。
所以**切换一个“恰好通过”的求解器并没有证明问题解决**，未部署fallback。
双线性深度与原3×3中位采样差约.8–1.0mm中位，仅是数值线索，不足断言
渲染异步、内参错误或单一深度采样是根因。

固定一次LK亚像素跟踪诊断（21窗口/3层/30次或.01收敛、双向≤1px，没有搜索）：
78原匹配73双向有效、72通过原深度与自身过滤；LK相对原SIFT位置修正中位
.264px、90分位.999px。**同一原joint/质量门**此段为42/72内点、.829/.829px，
深度误差8.788mm，暂时通过。但这只表明匹配定位噪声有贡献；估计Z位移仍
约−9.6mm、深度误差比原更大，缺少运动真值，不能说精度已提高或根因全解。
全部诊断不发新模型/控制；证据在本地`h22_failure_probe`的三个JSON与误差图，
跨SciPy复算原像素尾差约1e-9，不改变生产1px门。

下一登记**H24≤1800s纯CPU**：只检验统一、默认关闭的双向LK匹配定位前端，
固定上述参数、不改joint目标/支持与质量门、不扩SEARCH恢复或场景预算；
单元边界、旧路径逐项不变，以及H22全部实际353段（含4恢复）统一重算，
报告全部退化/恢复和位移差，不能挑末段。若整体退化或安全边界不清即不部署；
必须独立审后再决定是否另开物理。父负责公共harness文件，子fa24采训冻结，
不让该探索阻塞其已有两新gripper门后的114989/120更新/六配对路线。
