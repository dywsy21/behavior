# H53b：关闭非actor旁观者相机的单次共享场景检查

负责人Codex；登记2026-09-24 21:55北京时间。源分支`feat/semantic-agent-grounded-20260918`，准确commit提交/运行后记入launch和plan；当前仅CPU实现，未部署或载场景。

## 唯一假设

H53未到任务reset就触显存门，且任务scene载入前已为默认旁观者相机建立RT渲染管线。已读冻结OG源码：`simulator.py:543`由`gm.RENDER_VIEWER_CAMERA`控制此相机；`macros.py`默认True、1280×720，headless不自动关闭它。这个viewer不是机器人head/left_wrist/right_wrist观测，官方Evaluator目录也没有viewer引用。

检验**只关闭该旁观者相机**能否在原H53预算内完整载入一个原始任务场景，保持三路actor RGB-D。它可能节省资源，但此前时间/显存关联不是精确分摊，效果须实测；不预设完整场景一定能装下。

## 不变项和预算

继承[H53](2026-09-24-h53-original-scene.md)的原始TRAIN138/seed0、R1Pro23D、同window/robot/source依赖SHA、已有Isaac5.1/OG3.9.1开发运行时、官方Session及外层reset→load138→reset、4render-only捕获、0actor/专家前缀/旧策略前缀/模型/训练。

只一次新进程/一Session，600s活动监管＋最多30s自有清理；GPU3主卡新增/自有≤4096MiB，辅助≤512MiB，启动四卡free≥7168、运行≥3072MiB，四原训练PID不变、未知PID停止自有worker。**不提高显存额度、不降低余量、不升级环境、不同时改DLSS或物理设置**。仍CPU72–75/Kit4线程、新的冷私有runtime/cache，不复制或复用H53缓存，因此时间不能当纯已预热对照。

三路机器人相机仍head720×720、双腕480×480、RGB+linear depth；初始化后不resize/remove任何相机、不移除其他传感器，不向actor提供viewer或隐藏状态。Evaluator正常物理初始化/settling照旧，不称0物理初始化。

## 实现与验收

`probe_scene_without_viewer.py`为独立入口，import无副作用；在CLI固定H53b路径/成功字段/预算描述与macros SHA。调用原scene worker时，在任何og.app/og.sim存在之前通过**仅本进程**官方macro设False；不改已安装文件、全局共享环境或活跃source。完成时必须实际`sim.viewer_camera is None`且macro仍False，两项进入严格成功字段；原H53入口默认不关闭viewer。

新增冻结`macros.py` SHA`5e8ebb1a00fa3e864c5a4984f126442e91b6653f5869977ecddbf4039ea73228`；原13项依赖继续校验。原one-shot桥、调用序列计数、三路RGB-D字节/shape/dtype/有效比例/真实4render/0control/关节不变/原资源门全部保留。

作者41 CPU通过（H53原36＋5新viewer隔离/真实缺失/参数边界）；独审待。双端同测/资源门后才提交唯一run，不直接复用H53输出。通过还须完整源/预算/资源/退出证据与三RAW人工核验；**仍不是success rate评测**，也不证明模型＋模拟器联合部署已可行。

21:55北京时间：独审闭合，41 CPU/语法/diff检查过，无实质阻塞。开始固定Git与远端同门，尚未启动。

## 预定位置

- run `/mnt/nvme_tmp/robodojo_agentic_20260924/h53b_no_viewer_v1`
- 私有runtime `/mnt/nvme_tmp/robodojo_sim_runtime_20260924/h53b_no_viewer_v1`
- 本地完整小包 `artifacts/agentic-vlm-goal-20260918/h53b_no_viewer_bundle_v1`

此票若仍超限，保留失败并重新诊断，不继续自动加显存额度。H53全部源/run/runtime及8件证据已封存97292f7，禁止覆盖；H53b实际结果待。

## 实际运行

固定源码`8f0812e5375d8331bb47d5628180b9e43f1e30c5`，远端独立`git_worktrees/shared_scene_8f0812e`，双端41 CPU/独审/14依赖与实时资源门过。唯一launch UTC13:56:48.554197，supervisor3473931；上述run/runtime现已创建，实际场景与终态待验，非通过。

## 实际终态与全包验收（22:11北京时间）

未通过。supervisor3473931/worker3473956已退出，监管562.945535665s、worker -15，错误`Incremental shared GPU memory cap exceeded`。末采样560.658536444s GPU3整卡77909MiB，相对73664基线新增4245MiB、自有4212MiB，均超过4096；free3244MiB仍高于3072，因此不是余量门或600s超时。四原训练全704个采样均各73644MiB并保留，退出free7489/7489/7489/7488。

GPU0/1/2/3自有峰456/416/416/4212MiB，整卡增量峰470/422/422/4245MiB，最低free7018/7068/7068/3244。实际GPU3唯一active，既有8设置/一次app与两copy no-op仍核真；无已安装源码写入。宏确实False，但未到最终`viewer_camera is None`验收，不把它写成完整通过。

OG欢迎页worker29.896s，`Imported scene 0`345.153s，然后robot初始化。主卡新增>1GiB在359.749s，537.677s跳至3455MiB（自有3422），560.659s到4245；Kit522.217s警告A100不支持当前DLSS-RR，545.460s出现`Replicator_01`创建。末日志仍有碰撞网格cooking，不能把全部新增精确归因到某一种缓冲。外层reset/load-instance事件、RGB-D捕获、actor控制/前缀/模型/训练均0，`success_rate=null`；没有图片可人工验收。

完整8件在本地`artifacts/agentic-vlm-goal-20260918/h53b_no_viewer_bundle_v1`，4主文件双端SHA一致：

- launch `bafd979c5ecd5faca4b5cafcae6b72004e655ae0e529ca570d68645e5fac2d15`
- supervisor `c0e026fd7621d4670203c21fe60d986afaac83bcd5324fae07733ec53c5d27db`
- worker `a4b93c6579f786eb2a24e98dea2c302415510c56e33bf4b2d5bfca827214b3ce`
- kit.log `ea910e5035963479897a66549697260e0c166ec51dd5a4feb9ab3312fc93818c`

下一只读定位发现：冻结OG `Simulator._set_renderer_settings:624–626`在启动后强设`RealTimePathTracing`/rt2=True，并同时开启reflections/indirectDiffuse/AO/sampledLighting；它不是“低质量模式就自动不使用DLSS”。正在核对该模式与A100已报告不支持的RR依赖及可用官方legacy渲染配置，不放宽显存，不自动重跑。任何替代渲染须单独登记/测试/独审，并保持原三相机分辨率、几何、光照与物理的明确变更边界，不能当同像素正式评测。
