# 项目协作规则

本文件必须纳入 Git，禁止加入 `.gitignore`。适用于本仓库及其子目录。

## 当前目标与实验尺度

- 当前是 **为之后50个任务的大训练筛选通用方法、排除实现错误的准备阶段**，不是已经进入全任务正式训练。五任务子集只作方法验证，不围绕几个已见实例无限调参。
- RL必须以跨50任务复用为设计目标：共享任务/技能条件化策略、统一奖励/结果接口和训练算法。端盘掉落只是诊断例子，禁止把计划收窄为端盘专用控制器或逐任务手写50套RL。先用少量异质任务验证通用性，详见`docs/RL_METHOD_PLAN.md`。
- 当前计划与分工见 `docs/TEAM_PLAN.md`，服务器路径见 `docs/SERVER_LAYOUT.md`，Git 协作见 `docs/COLLABORATION.md`。先读这三份文件，再动手。
- 每次实验先写清：一个主要假设、唯一负责人、代码 commit、权重/数据版本、任务/实例/seed、GPU/时间/步数上限、对照及停止条件。到预算即停止评估是否值得继续，不自动追加一轮5000步。
- 默认先单元检查和少量局部闭环，再决定是否扩展。小规模无提升不等于方法被否定；有局部提升也不等于完整任务成功率提升。
- 不为迁移仓库、整理文档或整理磁盘启动新训练。全任务训练、多轮搜索、大规模数据重建须由团队明确批准后实施。

## Git 是代码和计划的唯一同步渠道

1. **每次开始工作先同步远端最新进度**：查看 `git status --short --branch`、`git remote -v`，在干净且有 upstream 的工作分支执行 `git pull --ff-only`；同时 `git fetch origin --prune` 检查 `origin/main` 的团队进展。新任务从最新 `origin/main` 建自己的分支。
2. 若有未提交修改、没有 upstream、分支分叉或合并冲突，不得强拉、覆盖、`reset --hard`、擅自 stash 他人修改或 force-push。先 fetch；保留当前内容，在自己的分支保存经过检查的工作或使用独立 worktree，再合并/协调。记录本次未能 pull 的原因，不假装已同步。
3. 远端为 `https://github.com/dywsy21/behavior.git`。各人使用独立分支，如 `feat/low-fm-*`、`feat/planner-*`、`feat/multitask-rl-*`；合入 main 前审查。常规同步走 commit/push/pull，不再用 rsync/scp 覆盖活跃源码目录。
4. 在服务器启动实验前，同样先 pull 对应实验分支，固定准确 commit，然后使用独立 worktree/源码快照运行。**禁止对正在被训练、服务或模拟器使用的源码目录执行 pull 或热改。** 从另一个干净 checkout 同步更新，旧运行完成后再切换。
5. 实验结束后提交小体积配置、必要修复和结果摘要，更新计划中的状态与证据路径，再 push。不要只把结论留在聊天里。推送前检查暂存文件、秘密和大文件；不要无检查 `git add .`。
6. 初始化迁移期间，按迁移清单保留原远端脏工作区和原 Git 历史；未核验前不得用本地副本覆盖它。

## 文件与磁盘

- Git 跟踪：源码、配置、测试、必要脚本、AGENTS.md、协作计划、轻量结果摘要/清单。数据、权重、环境、缓存、原始日志、视频、海量诊断文件、旧工作快照不入 Git。
- 数据和权重仍在服务器，通过路径、版本和 SHA 关联；禁止为了 Git 同步把 TB 级文件复制到仓库或提交 Git/LFS。
- `artifacts/` 等被忽略目录不是垃圾桶：其中的唯一数据、唯一权重、视频和未归档结果也不能随意删除。
- 清理前检查实际挂载、可用空间、运行进程和引用；不能只凭目录名字、旧时间或“有副本”判断可删除。
- 优先迁移闲置副本到 `/mnt/tmp1` 的明确归档目录，跨盘复制校验完成后再移走源副本。记录原路径、新路径、大小、校验、依赖和恢复方式；必要时保留兼容软链。
- 不移动或删除正在使用的源码、数据、模型、解释器、资源及模拟器缓存；不动队友任务。禁止对根目录、工作区根、HOME 或未解析变量执行递归删除。

## 模型、数据与评测底线

- 保留高层规划/记忆与低层连续 FM 的分离；高层离散技能的生成不具有可直接穿越的 FM 梯度。
- 意图—动作监督必须来自同一状态下实际执行的技能；错误子目标不能搭配另一技能的专家动作做正向 BC。
- 保持机器人真实23维控制、模型27维表示的映射；该 R1Pro 约定的补齐位为 `[7,8,17,18]`，底盘/躯干是真实控制，不可屏蔽。变更 embodiment 时从元数据推导，不盲目复用常数。
- 图像历史与动作历史是两回事。当前 A3 是6帧历史图像、未来32动作、执行前16动作，动作执行起点为0；不得再以 `num_obs_steps-1` 推断动作起点。训练/推理 mask、归一化、动作时钟必须做实际回归。
- 原逐task 5%留出集、public_test及派生近重复样本不回灌训练；新分支轨迹按来源实例分组切分。已反复诊断的 public_test301 是开发集，不称独立盲测。
- 仿真特权状态可生成训练奖励/标签及诊断，不能直接进入部署 actor/高层输入。不能把标注区间结束、夹爪闭合、超时或模型自报当作成功/失败真值。
- CE/FM loss、局部技能完成率、完整任务成功率分开报告；完整任务成功只接受官方终止/物理判据，且预算、实例、seed、重置规则一致。
- 新构造数据须人工分层抽查图像/视频和对应标签，并保留可定位记录。缺失类别如实注明，不造 FAILED/SUCCEEDED 标签填数。
- 三人修改独立模块；接口变更先写清并协调。代码合入前由另一成员/独立审查检查；不覆盖他人 dirty/untracked 工作。
